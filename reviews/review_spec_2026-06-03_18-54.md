# Gemini-style Review — spec lane

- **When:** 2026-06-03T18:54:22
- **Provider:** OpenCode Zen (`gemini-3.1-pro`)
- **Elapsed:** 0.0s
- **Attached files (6):**
  - `CLAUDE.md` (13,782 chars)
  - `06_Lift_Recommendations_2026-05-30.md` (19,428 chars)
  - `Progress/index.html` (33,236 chars)
  - `Briefings/gemini_deck_prompt_2026-06-03.md` (7,716 chars)
  - `Briefings/gemini_architecture_prompt_2026-06-03.md` (6,595 chars)
  - `00_Architecture_and_Roadmap.md` (7,178 chars)

---

## Response

### Concerns
(MOCK MODE — OPENCODE_API_KEY is not set; this is a placeholder so the
audit-trail file still gets written. See the module docstring for where to
put your OpenCode Zen API key.)

### Missed alternatives
- None evaluated.

### Affirmations
- None evaluated.

### Concrete suggestions
- Set OPENCODE_API_KEY in your shell (see top of review.py) and re-run.

### One question for the team
- Which model in the OpenCode Zen catalog do we want as the default reviewer?
  (script default: gemini-3.1-pro — canonical Gemini per CLAUDE.md ritual)


---

<details>
<summary>Full prompt sent (click to expand)</summary>

```
You are reviewing engineering decisions made in a working session on the "Family Inc"
household-automation project. You are the explicit adversarial-but-fair reviewer. The
team owners (Adar = CTO, Shanee = Chief Design + PO) value pushback over agreement.

## Project one-paragraph context
Adar's household-automation system. Master DB = Google Sheets. PWA dashboard pinned
to iPhone, write-back to the Sheet. Briefings via WhatsApp (Twilio). Israeli context
(ILS, Hebrew, Maccabi healthcare). Family = 2 adults + 2 kids (kid-1 3yr, kid-2 3mo).
Operating principles: briefings > notifications, alert budget 2/day, no kid-facing UI,
boring tech, one source of truth per domain.

## What this session changed
- Progress-review session (PO leading): computed a fresh status model from CLAUDE.md, 06_Lift_Recommendations, 00_Architecture, the three specs, Automation/README and Setup/00_Runbook; PO approved it before deliverables were built.
- Status-model corrections to stale records: Hebrew chrome strings are DONE (2026-06-01, snapshot said pending); item 4.7 (Tasklet replies) is approved-NOT-built despite the 06-doc's ✅; Phase 6.1 dashboard UI + engine tombstone guard are spec-only.
- New: `Briefings/gemini_deck_prompt_2026-06-03.md` — copy-paste Gemini PRO prompt for a 6-slide progress deck; status model baked in as data (3-state honesty: done / built-dark / spec-only); per-slide visual specs; 3 PO decisions (Twilio go/no-go, devops day, family-group criticals).
- New: `Briefings/gemini_architecture_prompt_2026-06-03.md` — copy-paste Gemini PRO prompt for an L0/L1/L2 architecture doc; Mermaid required at every level with a mandatory "not built" overlay (solid/dashed/dotted); one-real-day data-flow walkthrough (mock vs full-operation, side by side).
- New: `Progress/index.html` — single-file vanilla HTML/JS/CSS progress prototype (no deps, opens from disk): phase timeline 0→6.1, 32-card backlog board filterable by state+week, credential dependency graph with SVG wires + highlight (Twilio = dominant node, 5 unlocks), built-vs-live meters for 7 lanes, ₪0→₪70/mo cost strip, tap-anything→doc-reference drawer. Status model is one inline JSON blob — future reviews edit only the blob.
- No production files touched; read-only session outside Progress/ + the two prompt files.
- Process note: review.py lacks a progress-review lane; this review ran with --lane spec + --extra-files as the closest fit. Adding the lane is a candidate follow-up (one-line choices change).

## What I want you to review
1. Architectural soundness of the changes above.
2. Missed alternatives or simpler paths we didn't consider.
3. Tradeoffs we made implicitly without writing them down.
4. Risks / failure modes not covered.
5. Internal consistency across the changed files.

## What I do NOT want you to review
- Style, tone, formatting, copyediting.
- Adherence to design "best practices" in the abstract — only call those out if
  ignoring them creates a concrete risk for THIS project.
- The roles or session ritual itself (out of scope; that's our process).
- Files I did not list in "What this session changed" — assume those are settled.

## Required output (use these headings, in this order)
### Concerns
Things that should change. Be specific (file + section). Rank by severity.

### Missed alternatives
Paths we likely didn't explore. One-sentence each. Don't develop them — just name them.

### Affirmations
Decisions you think are correct, especially non-obvious ones. Brief.

### Concrete suggestions
Edits we could make right now. Phrase as "replace X with Y because Z."

### One question for the team
The single most useful question you'd ask Adar+Shanee+Claude if you had one.

Be terse. We're going to act on this directly.

---

## Attached context files

The following files are attached for you to read. Each is delimited by a header line.
Reference them by relative path in your review.

=== File: CLAUDE.md ===
# Family inc. — Project Context

*Auto-loaded at the top of every session opened in this folder. Keep tight; long-form thinking lives in the numbered docs.*

## What this is

Adar's household-automation system. Source of truth = Google Sheets `Family_OS` (tabs per domain) + Drive folder structure. Briefings via WhatsApp (Twilio). PWA dashboard pinned to iPhone, write-back to the Sheet. Audience = Adar + Shanee. Kids' data is structured but adult-mediated. Israeli context throughout (ILS, Hebrew, Maccabi, etc.).

## Roles

| Role | Person | Owns |
|---|---|---|
| CTO + co-PO | **Adar** | Engineering direction, stack calls, ships code; joint product authority with Shanee |
| Chief Design + co-PO | **Shanee** | Product direction, UX feel; joint product authority with Adar |
| Lead Architect | **Claude** | System design, code/specs, tradeoff proposals — defers to either PO on product calls; defaults to Adar's preference on engineering details |
| Reviewer (adversarial but fair) | **Gemini** | Independent second opinion at session end — not a co-author, explicitly outside our context |

**Decision authority.** Adar and Shanee are joint POs. Either can start a session and take decisions on their own; the other does not need to be in the loop. Routine calls (copy, ordering, what's in a drawer, which color, what to ship next) land with whoever is leading the session. Major directional calls (new feature, principle change, removing something already shipped) are made jointly and recorded in the affected doc. Disagreements are surfaced, not papered over.

**Session leadership.** Whoever pastes the session-start prompt leads that session. Claude treats the leader as "the PO" for the duration and doesn't ask the other partner to weigh in unless the leader explicitly defers.

## Stack & file map

```
Family Inc/
├── CLAUDE.md                              ← this file (auto-loads)
├── 00_Architecture_and_Roadmap.md         ← north star + phase status
├── 01_Family_Kickoff_Guide.md             ← kickoff script
├── 02_Kickoff_Output_2026-05-30.md        ← real kickoff output (goals + owners)
├── 02_Reminders_Engine_Spec.md            ← reminders engine spec
├── 03_Partner_Signoff.md                  ← partner signoff doc
├── 04_Dashboard_Design_Prompt.md          ← dashboard design brief
├── 05_Dashboard_Design.md                 ← dashboard design (rev 2026-05-30: arc + ticker + skeleton + offline lock)
├── 06_Lift_Recommendations_2026-05-30.md  ← LIVE BACKLOG with ✅/🟢/🟡/❌ status
├── 07_WhatsApp_Group_Summarizer_Spec.md   ← WhatsApp groups feature spec (built 2026-06-02, mock mode)
├── 08_Design_Session_Prompt.md            ← design session kickoff prompt
├── 09_Progress_Review_Session_Prompt.md   ← progress review kickoff (deck + architecture + Progress/ prototype)
├── Dashboard/                             ← vanilla HTML/JS PWA prototype
│   ├── index.html, app.js, styles.css
│   ├── config.js, mock_data.json
│   └── README_SETUP.md                    ← OAuth setup
├── Automation/                            ← Python automation scripts
│   ├── README.md                          ← script index + cron suggestions
│   ├── hebcal_client.py                   ← Shabbat + chagim
│   ├── friday_briefing.py                 ← Claude-reads-whole-sheet briefing
│   ├── anomaly_detector.py                ← subscription/duplicate/drift
│   ├── pediatric_milestones.py            ← Tipat Halav schedule
│   ├── goal_coaching.py                   ← weekly retro one-liners
│   ├── hebrew_categorizer.py              ← merchant → category
│   ├── pdf_to_event.py                    ← Ohai-style multimodal
│   ├── dira_tracker.py                    ← Dira BeHanacha scraper
│   ├── review.py                          ← session-end Gemini review (USE THIS, not hand-assembled prompts)
│   ├── whatsapp_summarizer.py             ← group msg classifier + tiered alerts + digest
│   └── whatsapp_bridge/                   ← self-hosted Baileys listener (Node, free)
├── Setup/                                 ← runbook for cred-dependent items
│   ├── 00_Runbook.md                      ← TOC
│   ├── 01_Israeli_Bank_Scrapers.md
│   ├── 02_Yad2_Madlan_Watchers.md
│   ├── 03_Gmail_MCP_Bill_Parser.md
│   ├── 04_Mindee_Receipt_OCR.md
│   ├── 05_iCloud_GCal_Sync.md
│   ├── 06_Voice_Receipt_Shortcuts.md
│   ├── 07_Maccabi_Passport_Forwarders.md
│   ├── 08_Israeli_Reminders_Seed.csv
│   ├── 09_Vaccine_Schedule_Seed.csv
│   ├── 10_Dashboard_Goals_Seed.csv
│   ├── 11_Kol_Zchut_Reference_Links.md
│   ├── 12_WhatsApp_Group_Config_Seed.csv  ← group routing + alert/critical keywords
│   └── code/                              ← drop-in scripts (.js, .gs)
├── Briefings/                             ← generated briefing outputs
├── Family_OS.xlsx                         ← master sheet template
├── reminders_engine.py                    ← legacy script (kept until ported)
└── sunday_briefing.py                     ← legacy script (kept until ported)
```

## Operating principles (non-negotiable)

- **One source of truth per domain.** No data lives in two places. If it does, one of them is a view.
- **Boring tech.** Google Sheets > custom DB. CSV drops > brittle scrapers. PWA > native.
- **Alerts are a budget, not a firehose.** Hard cap = 2/day. WhatsApp is the only alert channel (PWA push declined 2026-05-30).
- **Chrome language: Hebrew default, English fallback toggle.** `<html lang="he" dir="rtl">` ships as default; `localStorage.familyinc.lang = "en"` flips to English. Data values (merchant strings, Kol-Zchut links) stay Hebrew regardless.
- **Offline write-back: queue + per-row tombstone, not lock.** Dashboard taps queue locally when offline; reconnect flushes them with `WriteQueue_Tombstone = now()`. Reminders engine reads tombstones on startup and skips rows tombstoned within 6h.
- **Briefings > notifications.** Daily 1-liner, Friday briefing, weekly Sunday review (Sat 21:00).
- **Partner-readable, kid-aware.** Both adults see everything. No kid-facing surface.
- **No money movement on Adar's behalf. No credential storage. No messages outside Adar+Shanee.**

## Where to look for what

| You need to know | Read |
|---|---|
| What we're trying to build | `00_Architecture_and_Roadmap.md` |
| What's done / pending / declined | `06_Lift_Recommendations_2026-05-30.md` (status legend at top) |
| Live backlog ordered by week | `06_Lift_Recommendations_2026-05-30.md` §"Ship order" |
| Dashboard contract | `05_Dashboard_Design.md` |
| Reminders engine contract | `02_Reminders_Engine_Spec.md` |
| Goals + owners + kids info | `02_Kickoff_Output_2026-05-30.md` |
| How to ship a cred-dependent item | `Setup/00_Runbook.md` → pick a section |
| What a Python automation does | `Automation/README.md` |

## DevOps — running and shipping the prototype

The interactive prototype at `Dashboard/` is the canonical design surface. To keep design sessions self-serve (either PO can run one without the other being available), the prototype is hosted as a live URL that both phones can hit at any time.

**Hosting:** GitHub Pages from the `main` branch, `/Dashboard/` subdirectory.

**Live URL:** `[TODO: Adar to set up GH Pages repo and paste URL here]`

**Working folder sync:** the `Family Inc/` folder lives on `[TODO: confirm — iCloud Drive recommended]` so both Adar and Shanee see the same files from their own machines.

**Session loop (whoever leads runs this):**

1. Open Cowork → load the `Family Inc/` folder.
2. Paste the session-start prompt (e.g. `08_Design_Session_Prompt.md` for a design session).
3. Claude reads `Dashboard/DESIGN_LOG.md` to see the prior session's state.
4. Decisions land as direct edits to `Dashboard/{index.html, app.js, styles.css, mock_data.json}`.
5. Cowork commits + pushes to `main` *(method: `[TODO: Adar — choose: auto-commit per save, manual `git push` at end of session, or session-end script]`).*
6. GitHub Pages redeploys; the live URL is fresh in ~30 seconds.
7. Leader refreshes their pinned PWA on phone or laptop → sees the change.
8. Loop until end-of-session.

**No live-machine dependency.** The live URL is always up; neither laptop needs to be on for the other PO to start a session.

**One-time setup checklist (Adar's responsibility, blocking Shanee's first solo session):**

- [ ] Create GH repo containing `Dashboard/` (root or `/docs/`)
- [ ] Enable GH Pages from `main`
- [ ] Decide and document the commit-on-save flow
- [ ] Pin the live URL as a PWA on both phones
- [ ] Paste the live URL into this file under "Live URL" above
- [ ] Confirm Family Inc folder sync method (iCloud Drive vs Google Drive vs Dropbox)

Until this checklist is complete, design sessions can still run locally — Claude edits files, leader opens `Dashboard/index.html` in a desktop browser. The phone-preview workflow waits on the GH Pages step.

## Phase status snapshot (2026-05-30)

- ✅ Phase 0 — schema, Drive structure
- 🟡 Phase 1 — Sunday briefing built; Calendar-Events tab live; Google Calendar connector not wired
- 🟡 Phase 2 — Reminders engine built (dry-run); Twilio not provisioned
- 🟢 Phase 3 — finance ingestion plan ready (`Setup/01_Israeli_Bank_Scrapers.md`)
- 🟢 Phase 4 — health/education seeds ready (`Setup/09_Vaccine_Schedule_Seed.csv`, etc.)
- 🟢 Phase 5 — goals seeded incl. Shanee's wellness goal
- ✅ Phase 6 — Dashboard PWA prototype shipped; design refresh applied 2026-05-30
- 🟢 Phase 6.1 — `LastDoneBy` + `DoneAt` + `WriteQueue_Tombstone` schema bump; rolling-7-day arc + domain-grouped ticker + skeleton + queue+tombstone offline model spec'd; engine addendum spec'd

---

# Session-end ritual — Gemini review

Every working session ends here. Don't close until this is done.

## Step 1 — Claude generates the Gemini prompt

At session end, Claude produces a copy-paste block in this exact shape:

```
You are reviewing engineering decisions made in a working session on the "Family Inc"
household-automation project. You are the explicit adversarial-but-fair reviewer. The
team owners (Adar = CTO, Shanee = Chief Design + PO) value pushback over agreement.

## Project one-paragraph context
Adar's household-automation system. Master DB = Google Sheets. PWA dashboard pinned
to iPhone, write-back to the Sheet. Briefings via WhatsApp (Twilio). Israeli context
(ILS, Hebrew, Maccabi healthcare). Family = 2 adults + 2 kids (kid-1 3yr, kid-2 3mo).
Operating principles: briefings > notifications, alert budget 2/day, no kid-facing UI,
boring tech, one source of truth per domain.

## What this session changed
[Claude fills in: a tight bullet list of decisions, files changed, specs landed.
One line per item. No defending — just what shifted.]

## What I want you to review
1. Architectural soundness of the changes above.
2. Missed alternatives or simpler paths we didn't consider.
3. Tradeoffs we made implicitly without writing them down.
4. Risks / failure modes not covered.
5. Internal consistency across the changed files.

## What I do NOT want you to review
- Style, tone, formatting, copyediting.
- Adherence to design "best practices" in the abstract — only call those out if
  ignoring them creates a concrete risk for THIS project.
- The roles or session ritual itself (out of scope; that's our process).
- Files I did not list in "What this session changed" — assume those are settled.

## Required output (use these headings, in this order)
### Concerns
Things that should change. Be specific (file + section). Rank by severity.

### Missed alternatives
Paths we likely didn't explore. One-sentence each. Don't develop them — just name them.

### Affirmations
Decisions you think are correct, especially non-obvious ones. Brief.

### Concrete suggestions
Edits we could make right now. Phrase as "replace X with Y because Z."

### One question for the team
The single most useful question you'd ask Adar+Shanee+Claude if you had one.

Be terse. We're going to act on this directly.
```

Claude fills in the "What this session changed" section with real specifics.

## Step 2 — Claude lists the context files to attach

Same message as Step 1, Claude appends:

> **Attach these to your Gemini chat:**
> - `00_Architecture_and_Roadmap.md` *(always)*
> - `06_Lift_Recommendations_2026-05-30.md` *(always — for backlog status)*
> - `CLAUDE.md` *(always — for principles + roles)*
> - *[lane-specific files that changed this session]*

Lane-specific defaults:

| Session worked on | Also attach |
|---|---|
| Dashboard UI/UX | `05_Dashboard_Design.md`, the three `Dashboard/*` files touched |
| Automation script | `02_Reminders_Engine_Spec.md` + the specific `Automation/*.py` files |
| Setup runbook | The specific `Setup/*.md` section |
| WhatsApp groups | `07_WhatsApp_Group_Summarizer_Spec.md` |
| New spec / new feature | Just the new doc + any docs it references |

## Step 3 — Adar runs the review externally

Adar pastes the prompt + files into a fresh Gemini chat, runs it, brings the output back to Claude in the next message.

## Step 4 — Resolve

Claude walks through Gemini's response and produces:

1. **Apply** — changes Claude agrees with, made directly to the affected files.
2. **Defend (with reason)** — places Claude disagrees, with a written justification appended to the relevant doc as a tradeoff note.
3. **Open** — items neither side can resolve without Adar/Shanee input — surfaced as a question.

The session closes with a one-paragraph summary appended to whatever spec/doc was the main artifact, in this shape:

> *Reviewed by Gemini 2026-MM-DD. Applied: [list]. Defended: [list with reasons]. Open: [list]. Tradeoffs accepted: [list].*

## When to skip the ritual

Tiny edits (typo fixes, single-line config changes, status-flag updates) don't need a review. Anything that changes a spec, ships code, or sets a direction does.

=== End: CLAUDE.md ===

=== File: 06_Lift_Recommendations_2026-05-30.md ===
# Family inc. — Lift Recommendations

*Research synthesis · 2026-05-30 · 4 parallel passes (Israel, plugins/MCPs, UX/design, AI)*
*Status updated 2026-05-30 — Adar approved with exclusions; new items added (§5 and §6).*

This doc lists every concrete change worth shipping. Each item: **what it is → what it unlocks → effort → source**. The top of each section is the highest-leverage move. The "Ship Order" at the bottom is the recommended sequencing.

## Status legend

- ✅ **APPLIED** — code/spec landed in this session, runbook in `Setup/` or scripts in `Automation/`
- 🟢 **APPROVED** — Adar approved, awaits live creds or scheduling
- 🟡 **PENDING** — needs Adar's decision (see notes)
- ❌ **DEFERRED** — explicitly skipped

**Session 2026-05-30 decisions:** §1 all approved except 1.12 ❌ and 1.13 ❌. §2 all approved except 2.7 ❌ (Adar declined — WhatsApp stays the only alert channel). §3 all approved. §4 all approved. Two new sections added: §5 WhatsApp group summarizer and §6 Shanee's grooming goal.

---

## 1. Israel-specific

### 1.1 ✅ `israeli-bank-scrapers` (Node cron) → replaces monthly CSV drop
Headless screen-scraper covering Hapoalim, Leumi, Discount, Isracard, Max, Cal. No Open-Banking license needed (uses your credentials). Schedule daily on a tiny VPS, append to **Finance — Transactions**. Biggest single ROI item in the whole list — kills the manual pain point.
Effort: **med (4 hrs)**. https://github.com/eshaham/israeli-bank-scrapers

### 1.2 ✅ Hebcal API → Shabbat times + chagim in every briefing
Free JSON/ICS for candle-lighting, parasha, all holidays, fast days. Use Kiryat Tivon coords for ⟨town⟩ area. Briefing line: *"Friday candle-lighting 19:21 · Shabbat ends 20:32."* Also drives "is tomorrow a chag?" so daycare/work plans don't surprise you.
Effort: **low (30 min)**. https://www.hebcal.com/home/195/jewish-calendar-rest-api

### 1.3 ✅ Yad2 + Madlan watchers — the "house goal" gets a heartbeat
Apify actors `swerve/yad2-scraper` and `swerve/madlan-analytics`. Filter ⟨town⟩, room count, price ceiling. Post matches to WhatsApp. Track median ₪/sqm trend on the dashboard so the house goal moves visibly each week.
Effort: **low (1 hr)**. https://apify.com/swerve/yad2-scraper · https://apify.com/swerve/madlan-analytics

### 1.4 ✅ Dira BeHanacha lottery tracker
Mechir LaMishtaken's successor. Up to 20% discount + ₪40-60K periphery grants. Lotteries open in waves. Scrape `dira.moch.gov.il` weekly for Northern district tenders. Direct line into the house goal.
Effort: **low (2 hrs)**. https://www.dira.moch.gov.il

### 1.5 ✅ Tipat Halav + National Immunization Registry (kid-2)
Vaccination registry is online since March 2025. For kid-2 (3 mo): 4-month vaccines next (DTaP-IPV-Hib-HepB + Rotavirus + PCV), then 6-month. No API — but a once-a-month manual sync into the Health tab + Reminders auto-fires.
Effort: **low (manual)**. https://me.health.gov.il/en/parenting/raising-children/immunization-schedule/babies-immunization-schedule/

### 1.6 ✅ Tofes 101 + reservist credits (annual, January)
2026 added temporary miluim nekudot zikui. Build a one-shot January reminder + auto-fill template from People tab (teudot zehut, ages, Shanee maternity status). Catches tax-refund opportunities the family would otherwise miss.
Effort: **low (1 hr)**. https://www.cwsisrael.com/israeli-tax-changes-2026-complete-guide/

### 1.7 ✅ Misrad HaRishui + TesTime — car-test SMS forward
Annual test SMS arrives ~6 weeks ahead with a TesTime booking link. Forward to a dedicated email, parse into Reminders. Same trick for license-renewal email.
Effort: **low (30 min)**. https://www.gov.il/en/service/car_licence_renewal

### 1.8 ✅ Maccabi → email/SMS forward + parse (no API)
Maccabi has no public API. The practical play: forward Maccabi confirmation emails/SMS to a Google Workspace label, parse with Apps Script into Health tab + Reminders. Covers kid-1's pediatrician slots, kid-2's DDH ultrasound, blood-test bookings.
Effort: **med (3 hrs)**. https://www.maccabi4u.co.il

### 1.9 ✅ Misrad HaPnim — passport tracker
Online status check for kid-2's Israeli passport; recurring reminders for adults' renewal expiries. Polish citizenship status via Urząd Stanu Cywilnego — manual, but auto-remind every 30 days.
Effort: **low (1 hr)**. https://www.gov.il/en/departments/population_and_immigration_authority

### 1.10 ✅ Hebrew/RTL chrome (default) with English fallback toggle
`lang="he" dir="rtl"` on root (default) · logical CSS (`padding-inline-start`) · week starts Sunday · `DD/MM/YYYY` · `Intl.NumberFormat('he-IL', {style:'currency', currency:'ILS'})` for "₪1,250" · wrap LTR runs in `<bdi>`. Heebo font (see §3.2). **Settings has a `עברית / English` toggle; `localStorage.lang = "en"` flips `lang`/`dir` and selects the English chrome strings.** Decision 2026-05-30 (post-Gemini-review): Hebrew chrome default, English fallback available; data values (merchant strings, Kol-Zchut links) stay in Hebrew regardless.
Effort: **lang/dir flip done; Hebrew chrome string translation pending (low — 2 hrs)**.

### 1.11 ✅ Kol-Zchut + Chaim V'Chessed deep-links per reminder
Add a `guide_url` column to Reminders. When a reminder fires, the "how to do this" guide is one tap away. Saves Shanee hunting through bureaucracy.
Effort: **low (ongoing)**. https://www.kolzchut.org.il

### 1.12 ❌ Inforu / Cellact as Israeli SMS fallback — DEFERRED by Adar 2026-05-30
If WhatsApp/Twilio stays unprovisioned, drop to InforuMobile SMS (Hebrew-supporting, ILS billing, ~$0.01-0.02/segment). Hot-swappable behind the existing alert function.
Effort: **med (2 hrs)**. https://apidoc.inforu.co.il

### 1.13 ❌ Finanda (Open Banking IL) — DEFERRED by Adar 2026-05-30
ISA-licensed Open-Banking data aggregator under Israel's 2021 Financial Information Service Law. Pulls bank + card data programmatically. Riseup shut down Dec 2025, so this is the leading replacement.
Effort: **med — but consumer eligibility unclear**. Use `israeli-bank-scrapers` today; revisit Finanda Q4 2026. https://www.finanda.com/en/

---

## 2. Plugins / MCPs / automation

### 2.1 ✅ Gmail MCP via Apps Script → bills auto-flow to Contracts/Transactions
Kanshi Tanaike's working Apps Script MCP server (protocol 2025-03-26) is drop-in for Cowork. Pair with a Claude extraction step that pulls amount/due-date/vendor from arnona/electric/internet/insurance emails. Populates Contracts + Reminders without touching Veryfi/Klippa-class spend.
Effort: **med (4 hrs)**. https://medium.com/google-cloud/gmail-processing-using-mcp-network-powered-by-google-apps-script-5ede2a25c94e

### 2.2 ✅ Mindee receipt OCR via Drive→Zapier→Sheets
Free tier, 96% accuracy, documented Zapier recipe: Drive upload → Mindee parse → Sheets row append. Klippa is the budget alternative if multilingual/Hebrew bites. Veryfi is overkill at $500/mo floor.
Effort: **low (2 hrs)**. https://zapier.com/shared/receipt-scan/54f6726fa0ce50ffc70ffd2de68d02684fb45a6e

### 2.3 ✅ iCloud → Google Calendar via public ICS share (free, one-way)
Shanee shares her iCloud calendar as a public URL → subscribe in Adar's Google Calendar → briefing sees both. Refresh ~10 min. Beats CalendarBridge ($5/mo) for the read-only briefing use case.
Effort: **low (15 min)**. https://www.onecal.io/blog/how-to-sync-apple-icloud-calendar-to-google-calendar

### 2.4 🟢 LlamaParse on insurance/contract PDFs
Drive watcher → LlamaParse → Claude extracts renewal date / premium / provider → Contracts tab. Reducto only if audit-grade matters; for a household's 20 PDFs LlamaParse is right.
Effort: **med (3 hrs)**. https://www.llamaindex.ai/llamaparse

### 2.5 🟢 WhatsApp delivery — stay on Twilio, plan migration
Meta's July 2025 pricing reshuffle plus Twilio's ~$0.005/msg markup makes the difference <$5/mo at family volume. Migrate to Meta Cloud API direct only when you want the cleaner template approval flow. Don't let the migration block §2.1-§2.4.
Effort: **migration = 4-6 hrs**. https://www.twilio.com/en-us/whatsapp/pricing

### 2.6 ✅ Voice capture: Hey Siri → Sheets row
`bcongdon/google-sheets-shortcut` (Apps Script webapp) is battle-tested. "Hey Siri, log expense" → dictation → Claude parses → row append. Wife-friendly. Pair with Whisper Memos if Hebrew dictation needs more accuracy.
Effort: **low (1 hr)**. https://github.com/bcongdon/google-sheets-shortcut

### 2.7 ❌ PWA web-push — DEFERRED by Adar 2026-05-30 (WhatsApp stays the only alert channel)
iOS 16.4+ supports Safari web-push for home-screen-installed PWAs. iOS 26 (2026) defaults home-screen sites to web-app mode. Use PWA push for "today" surfaces; keep WhatsApp for the Sunday briefing and true alerts.
Effort: **med (6 hrs)**. https://www.magicbell.com/blog/pwa-ios-limitations-safari-support-complete-guide

### 2.8 ✅ Steal Ohai.ai's "PDF/photo → calendar event" pipeline
Ohai's killer feature: snap a daycare flyer, get a calendar event + reminder. Replicable with Gemini multimodal + your Drive watcher in ~5 hrs. Probably the single feature most likely to win Shanee's daily use.
Effort: **med (5 hrs)**. https://www.ohai.ai/

---

## 3. Design / UX lift

### ✅ 3.1 Palette swap: warm-paper neutrals + indigo accent
The current green (#2d5f3f) reads "earnest startup." 2026 calm-tech is on warm-paper neutrals with one distinctive accent:

- Surface light: `#FAF8F5` · dark: `#15161A`
- Ink: `#1A1A1F` / `#E8E6E1`
- Muted: zinc `#71717A` `#A1A1AA` `#D4D4D8`
- Accent: **Linear-indigo `#5E6AD2`** (replaces green)
- Semantic: success `#3F8F5F` sage · warning `#C58B3A` amber · alert `#C44545` terracotta

### ✅ 3.2 Type stack: Inter + Heebo + Geist Mono
Inter for Latin UI (tabular figures = decimals align automatically). Heebo for Hebrew (9 weights, same metric feel as Inter — feels like one system). Geist Mono for amounts only ("₪4,280.00") so money rows are visually distinct without changing weight.

### ✅ 3.3 Card pattern: "Big number + sparkline" (Notion 3.4 / 2026)
Each domain drawer's closed state = one oversized KPI + a sparkline underneath. Tap → details. Money, Goals, Health all become legible in one glance. Add Robinhood's open-source `spark` library for drag-to-scrub on the sparkline — the cheapest single trick that makes a chart feel real. https://github.com/robinhood/spark

### ✅ 3.4 Beeminder-style "bright line" for long goals
Progress bars are dead for 3-5 year goals — they never visibly move. Draw a target line + actual line + colored safety band ("ahead / on pace / behind"). Apply to House fund, Career, Passive income. https://www.beeminder.com/strava

### ✅ 3.5 Sunday briefing = Strava year-in-sport meets Morning Brew
Vertical scroll of 5-6 "scenes": *This week's spend · Kids' biggest moment · Next week's three things · One goal nudge · One contract heads-up.* Plain typography, one accent color, one-line items. Text *is* the design.

### ✅ 3.6 Lean dense, but progressively disclose
2026 consensus: Notion/Linear/Stripe density wins on dashboards; Things-style whitespace loses. Family inc.'s move = dense Today (every domain visible) + drawers (every detail hidden until tap). You already have this — keep going.

### ✅ 3.7 "All caught up" empty state
When Today has nothing: sage-tinted card with the date + warm one-liner ("Nothing on fire. Tea? ☕"). Zero CTAs. The absence is the reward.

### ✅ 3.8 Liquid Glass icon (cheap iOS-native feel)
Apple auto-applies the iOS 26 glass shimmer to your `apple-touch-icon` — pay-once configure-once trick to make the home-screen icon look native without faking translucency inside the app.
https://branclon.com/blog/liquid-glass-pwa-icons/

### ✅ 3.9 Sticky status pill in lieu of Dynamic Island
PWAs can't claim the island. Use a sticky top one-liner instead: *"Sunday briefing ready · 2 alerts."*

---

## 4. AI / agentic lift

### ✅ 4.1 Friday family briefing (Claude reads the whole Sheet)
Scheduled Cowork task Friday 17:00 → pull last 7 days from all tabs (~30k tokens) → Claude Sonnet 4.6 writes a 5-sentence briefing → WhatsApp. Cost: ~$0.10/week. Replaces today's templated Sunday script with something context-aware. Honors "briefings > notifications" rule.
Effort: **1 day**.

### ✅ 4.2 Hebrew merchant categorizer (cache + LLM)
Claude leads Hebrew classification (~93% on 11-cat financial task). Architecture: regex/exact-match cache first ("שופרסל"→Groceries), LLM fallback for unknowns, write back to cache. Skips Plaid entirely.
Effort: **1-2 days**.

### ✅ 4.3 Receipt photo → Sheet via Shortcut
Share-sheet Shortcut: photo → Claude vision "extract vendor, total, date, category" → webhook → Sheets append. Same trick as §2.6 but for receipts. 30-second capture, wife-friendly.
Effort: **half day**.

### ✅ 4.4 Subscription creep + duplicate detector
Python diff on Transactions: flag (a) new strings appearing ≥2 months in a row, (b) same vendor + same amount within 48h, (c) amount drift (Netflix +12%). Rule-based, no LLM, no spam. Surface in Sunday briefing.
Effort: **1 day**.

### ✅ 4.5 Goal-coaching agent — weekly retro pattern
Avoid daily nudges (2.5hr/wk lost, per research). Weekly Sunday: one line per Goal — *"House fund: +4,200 ILS, on track for 2028"* or *"Career: no movement in 3 weeks — add a task?"* One question max. Respects the calm budget.
Effort: **1 day**.

### ✅ 4.6 Pediatric milestone monitor (passive, non-scary)
Monthly task: given kid age (3yr / 3mo), Claude checks CDC milestones + Israeli Tipat Halav schedule, flags only what's due this month. Design rule: never list red flags unsolicited; surface "vaccine X due by [date]," not "watch for autism signs."
Effort: **1-2 days**.

### ✅ 4.7 Tasklet-style actionable briefing replies
Shortwave's Tasklet (Jan 2026) pattern: WhatsApp briefing arrives with inline action buttons. Reply "1" to add reminder, "2" to schedule, "3" to dismiss. Twilio webhook routes back to Claude → Sheet write. Briefings → still calm, but actionable.
Effort: **2 days**.

### ✅ 4.8 Anomaly detection beyond the 500-ILS threshold
Rolling 90-day mean+stddev per category. Flag >2σ ("Groceries this week: 1,400 vs 850 avg"). Merchant-string drift collapse ("שופרסל דיל" + "שופרסל אקספרס" → one).
Effort: **1 day**.

---

---

## 5. WhatsApp group summarization (new — Adar request 2026-05-30)

### 5.1 ✅ Read groups + classify ALERT / DIGEST / ROUTINE — BUILT 2026-06-02 (mock mode)
Full spec lives in `07_WhatsApp_Group_Summarizer_Spec.md`. Phases A–E built: self-hosted **Baileys** bridge (`Automation/whatsapp_bridge/`) → inbox JSONL → `Automation/whatsapp_summarizer.py` classifies with Claude Haiku (deterministic fallback), applies 5 hard rules, routes ALERTs **per group** under the 2/day budget, and builds the daily digest. Mock-tested against the spec's verify checklist (all pass).

Why it matters: daycare parent chats at 22:00 ("tomorrow bring snack") + building/vaad messages get missed. This closes that hole without adding screen time.

Bridge decision: **self-hosted Baileys (free)**, not Whapi — Adar chose free + privacy-first. Cost now ~**$9/mo** (Claude classification only; bridge is free software on an always-on machine). Groups onboarded: daycare, building, neighborhood, family, student. **Remaining to go live:** pair the Baileys QR once + swap `dispatch_alert()` to Twilio. Phase F (weekly accuracy tuning) not built.

---

## 6. Shanee's grooming + wellness goal (new — Adar request 2026-05-30)

### 6.1 ✅ Added as Goal 4 in the Goals tab
Fitness, weight, diet, skin, nails, hair — one integrated personal-habit goal owned by Shanee. Seeded in `Setup/10_Dashboard_Goals_Seed.csv` and `02_Kickoff_Output_2026-05-30.md`. Metric: weeks-at-cadence (target 52 by end of 2026). 90-day milestone: 3x/week routine sustained 8 consecutive weeks.

Tracked weekly in Friday/Sunday briefing through `Automation/goal_coaching.py` — surfaces "Wellness: 4 weeks at cadence, on pace" or "Wellness: missed last week — want a small reset?" One-line, never preachy.

Why it matters: until now, Goals tab was all Adar's career/passive-income work. Adding Shanee's personal goal makes the dashboard a shared system, not Adar's project Shanee tolerates.

---

## Ship order (recommended)

**Week 1 — kill the biggest pain points**

1. `israeli-bank-scrapers` cron (1.1) — replaces the manual CSV drop
2. Hebcal in Sunday briefing (1.2) — 30 minutes, immediate "wow"
3. iCloud → GCal public ICS (2.3) — 15 minutes, makes briefing real
4. Voice + receipt Shortcuts (2.6 + 4.3) — wife-facing wins

**Week 2 — close the loops**

5. Gmail MCP bill parser (2.1) — Contracts tab fills itself
6. Hebrew merchant categorizer (4.2) — Finance tab quality
7. Friday briefing via Claude reads-whole-sheet (4.1) — replaces templated script

**Week 3 — house goal gets a heartbeat**

8. Yad2 + Madlan watchers (1.3) + Dira BeHanacha tracker (1.4)
9. Beeminder bright-line goal viz (3.4)

**Week 4 — design refresh**

10. Palette swap to warm-paper + indigo (3.1)
11. Inter + Heebo + Geist Mono (3.2)
12. RTL guardrails (1.10)
13. Big-number + sparkline cards (3.3)

**Later — when the system is humming**

- LlamaParse on insurance PDFs (2.4)
- Tasklet-style reply-to-act (4.7)
- WhatsApp Cloud API direct migration (2.5)
- Pediatric milestone monitor (4.6)
- Tofes 101 January reminder (1.6)

---

## What to skip

- **Superhuman / Shortwave subscriptions** ($30/mo each) — rebuildable with Claude + Gmail MCP for ~$0.
- **Plaid-style enrichment** — LLM categorizer is cheaper, handles Hebrew better.
- **Daily-nudge apps** (Stoic-class) — violates *briefings > notifications*.
- **Faking Dynamic Island / Live Activities** in a PWA — Apple-only, gives a worse UX than a sticky status pill.
- **Finanda right now** — eligibility unclear for a household user. Revisit Q4 2026 when BOI expands open-banking to insurance/gemel.

---

*Source agents: 4 parallel research passes 2026-05-30. URLs inline. Full per-agent reports preserved in session memory.*
*Updated 2026-05-30: status marks applied; WhatsApp groups (§5) and Shanee's wellness goal (§6) added.*

---

## Reviewed by Gemini 2026-05-30 — Resolved

**Applied at this doc's level:**
- §1.10 Hebrew/RTL chrome confirmed as **default** with English fallback toggle (Adar's call: O1 = a). Updated to reflect the explicit toggle pattern.
- §5.1 WhatsApp summarizer: bridge recommendation switched from Whapi.cloud to self-hosted Baileys-on-dedicated-Android for privacy. See full revision in `07_WhatsApp_Group_Summarizer_Spec.md`.

**Resolved at the design-doc level (see `05_Dashboard_Design.md`):**
- Progress arc language scrubbed of streak/pace nag → rolling 7-day count.
- Appreciation ticker regrouped by **domain** primary, partner name inline (Adar's call: O2 = c).
- Explicit offline lock replaced with **queue + per-row tombstone**, the middle path Gemini proposed (Adar's call: D1 = accept). New `WriteQueue_Tombstone` column on Reminders; engine reads it on startup and skips rows tombstoned within 6h. Engine-side spec updated in `02_Reminders_Engine_Spec.md` §"Phase 6.1 addendum."

**Defended:** None.

**Open:** None.

**Tradeoffs accepted (logged 2026-05-30):**
- Hebrew chrome strings need translation work (~2h follow-up); the lang/dir flip ships now, the string set fills in incrementally.
- 6h tombstone window leaves a residual race: phone offline >6h with a queued completion → one extra alert before flush. Window is tunable.
- Domain-grouped ticker still surfaces names; we are betting domain primacy + partnership absorb the residual scoring risk.

=== End: 06_Lift_Recommendations_2026-05-30.md ===

=== File: Progress/index.html ===
<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Family Inc — Progress Review</title>
<style>
  :root{
    --paper:#FAF8F5; --ink:#1A1A1F; --muted:#71717A; --muted2:#A1A1AA; --line:#E4E1DC;
    --accent:#5E6AD2; --done:#3F8F5F; --dark:#C58B3A; --spec:#8C8FA3; --deferred:#C44545;
    --card:#FFFFFF; --mono:"Geist Mono","SF Mono",Menlo,monospace;
  }
  *{box-sizing:border-box;margin:0;padding:0}
  body{background:var(--paper);color:var(--ink);font:15px/1.45 Inter,Heebo,-apple-system,"Segoe UI",sans-serif;padding-bottom:80px}
  header{padding:28px 28px 10px}
  header h1{font-size:22px;font-weight:700}
  header .sub{color:var(--muted);font-size:13px;margin-top:4px}
  .headline{margin:14px 28px 0;padding:10px 14px;border-left:3px solid var(--dark);background:#fff;border-radius:0 8px 8px 0;font-size:14px}
  section{padding:26px 28px 6px}
  h2{font-size:15px;font-weight:700;letter-spacing:.04em;text-transform:uppercase;color:var(--muted);margin-bottom:14px}
  .legend{display:flex;gap:14px;flex-wrap:wrap;font-size:12px;color:var(--muted);margin-bottom:14px}
  .legend span::before{content:"●";margin-right:5px}
  .lg-done::before{color:var(--done)} .lg-dark::before{color:var(--dark)}
  .lg-spec::before{color:var(--spec)} .lg-def::before{color:var(--deferred)}

  /* Phase timeline */
  .timeline{display:flex;gap:6px;overflow-x:auto;padding-bottom:6px}
  .phase{flex:1;min-width:96px;background:var(--card);border:1px solid var(--line);border-radius:10px;padding:10px;cursor:pointer;border-top:4px solid var(--spec);transition:transform .08s}
  .phase:hover{transform:translateY(-2px)}
  .phase.done{border-top-color:var(--done)} .phase.dark{border-top-color:var(--dark)} .phase.spec{border-top-color:var(--spec)}
  .phase .pnum{font:600 11px var(--mono);color:var(--muted)}
  .phase .pname{font-size:13px;font-weight:600;margin:3px 0}
  .phase .pstate{font-size:11px;font-weight:600}
  .phase.done .pstate{color:var(--done)} .phase.dark .pstate{color:var(--dark)} .phase.spec .pstate{color:var(--spec)}

  /* Filters + board */
  .filters{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:14px}
  .chip{border:1px solid var(--line);background:#fff;border-radius:999px;padding:5px 13px;font-size:12.5px;cursor:pointer;color:var(--muted)}
  .chip.on{background:var(--accent);border-color:var(--accent);color:#fff}
  .board{display:grid;grid-template-columns:repeat(auto-fill,minmax(230px,1fr));gap:10px}
  .card{background:var(--card);border:1px solid var(--line);border-radius:10px;padding:11px 13px;cursor:pointer;border-left:4px solid var(--spec)}
  .card:hover{border-color:var(--accent)}
  .card.done{border-left-color:var(--done)} .card.dark{border-left-color:var(--dark)}
  .card.spec{border-left-color:var(--spec)} .card.deferred{border-left-color:var(--deferred);opacity:.65}
  .card .cid{font:600 10.5px var(--mono);color:var(--muted2)}
  .card .ct{font-size:13.5px;font-weight:600;margin:2px 0 5px}
  .card .cm{font-size:11.5px;color:var(--muted)}
  .badge{display:inline-block;font-size:10.5px;font-weight:700;padding:1px 8px;border-radius:999px;margin-right:5px}
  .b-done{background:#E5F2EA;color:var(--done)} .b-dark{background:#F6ECDC;color:#9A6E2D}
  .b-spec{background:#ECECF1;color:#5E6070} .b-deferred{background:#F6E2E2;color:var(--deferred)}
  .b-week{background:#EAEBF8;color:var(--accent)}

  /* Dependency graph */
  .depwrap{position:relative;display:flex;justify-content:space-between;gap:40px}
  .depcol{display:flex;flex-direction:column;gap:8px;flex:1;z-index:1}
  .depcol h3{font-size:11px;font-weight:700;letter-spacing:.05em;text-transform:uppercase;color:var(--muted2);margin-bottom:2px}
  .dep{background:var(--card);border:1px solid var(--line);border-radius:9px;padding:8px 12px;cursor:pointer;font-size:13px}
  .dep:hover,.dep.hl{border-color:var(--accent);box-shadow:0 0 0 2px #5E6AD230}
  .dep .de{font-size:11px;color:var(--muted)}
  .dep.keynode{border-width:2px;border-color:var(--accent)}
  .unlock{background:var(--card);border:1px solid var(--line);border-radius:9px;padding:7px 12px;font-size:12.5px;cursor:pointer}
  .unlock.hl{border-color:var(--dark);box-shadow:0 0 0 2px #C58B3A30}
  svg.wires{position:absolute;inset:0;width:100%;height:100%;z-index:0;pointer-events:none}
  svg.wires path{stroke:#D4D4D8;stroke-width:1.5;fill:none}
  svg.wires path.hl{stroke:var(--accent);stroke-width:2.5}

  /* Lanes */
  .lane{background:var(--card);border:1px solid var(--line);border-radius:10px;padding:12px 14px;margin-bottom:9px;cursor:pointer}
  .lane:hover{border-color:var(--accent)}
  .lane .ln{display:flex;justify-content:space-between;font-size:13.5px;font-weight:600;margin-bottom:7px}
  .lane .ln span:last-child{font:600 11.5px var(--mono);color:var(--muted)}
  .bar{height:7px;border-radius:4px;background:#EFEDE9;margin:4px 0;position:relative;overflow:hidden}
  .bar i{position:absolute;inset:0 auto 0 0;border-radius:4px}
  .bar.built i{background:var(--dark)} .bar.live i{background:var(--done)}
  .barlab{font-size:10.5px;color:var(--muted2);display:flex;justify-content:space-between}

  /* Cost strip */
  .costs{display:flex;gap:14px;flex-wrap:wrap}
  .costcol{flex:1;min-width:230px;background:var(--card);border:1px solid var(--line);border-radius:10px;padding:14px;cursor:pointer}
  .costcol h3{font-size:12px;color:var(--muted);font-weight:600;margin-bottom:6px}
  .costcol .big{font:700 26px var(--mono)}
  .costline{display:flex;justify-content:space-between;font-size:12px;color:var(--muted);margin-top:4px}
  .costline b{font-family:var(--mono);color:var(--ink)}

  /* Drawer */
  #ov{position:fixed;inset:0;background:#1A1A1F66;opacity:0;pointer-events:none;transition:opacity .15s;z-index:9}
  #ov.open{opacity:1;pointer-events:auto}
  #drawer{position:fixed;right:0;top:0;bottom:0;width:min(400px,92vw);background:#fff;box-shadow:-8px 0 30px #0002;transform:translateX(105%);transition:transform .18s;z-index:10;padding:24px;overflow-y:auto}
  #drawer.open{transform:none}
  #drawer .dk{font:600 11px var(--mono);color:var(--muted2)}
  #drawer h3{font-size:17px;margin:4px 0 10px}
  #drawer .dnote{font-size:13.5px;color:var(--ink);margin-bottom:14px;white-space:pre-line}
  #drawer .dref{font-size:12px;background:var(--paper);border:1px solid var(--line);border-radius:8px;padding:9px 11px;margin-bottom:6px}
  #drawer .dref b{font-family:var(--mono);font-size:11.5px}
  #dx{position:absolute;top:14px;right:16px;border:0;background:none;font-size:20px;cursor:pointer;color:var(--muted)}
  .heb{direction:rtl;unicode-bidi:isolate}
  footer{padding:24px 28px;color:var(--muted2);font-size:11.5px}
</style>
</head>
<body>

<header>
  <h1>Family Inc — Progress Review</h1>
  <div class="sub">Status model as of <span class="mono" id="asof"></span> · internal PO tool · data lives in the inline JSON blob only</div>
</header>
<div class="headline"><b>Headline:</b> nothing is live yet. The skeleton is real — schema, dashboard prototype, 10 working scripts — but everything runs mock/dry. Code is not the bottleneck; credentials are.</div>

<section>
  <h2>Phase timeline 0 → 6.1</h2>
  <div class="legend"><span class="lg-done">done / live</span><span class="lg-dark">built-but-dark</span><span class="lg-spec">spec only</span></div>
  <div class="timeline" id="timeline"></div>
</section>

<section>
  <h2>Backlog board — every 06-doc item</h2>
  <div class="filters" id="filters"></div>
  <div class="board" id="board"></div>
</section>

<section>
  <h2>Dependency graph — what unblocks what</h2>
  <div class="legend"><span style="color:var(--muted)">tap a credential to highlight its unlocks · Twilio is the dominant node (5 lanes)</span></div>
  <div class="depwrap" id="depwrap">
    <div class="depcol" id="credcol"><h3>Credentials / one-time setup</h3></div>
    <div class="depcol" id="unlockcol"><h3>What goes live</h3></div>
    <svg class="wires" id="wires"></svg>
  </div>
</section>

<section>
  <h2>Distance to live — per lane</h2>
  <div id="lanes"></div>
</section>

<section>
  <h2>Cost strip — ₪ / month</h2>
  <div class="costs" id="costs"></div>
</section>

<footer>Built 2026-06-03 by Claude (Lead Architect) · progress-review session · sources: 00 / 02 / 05 / 06 / 07 docs, Automation/README.md, Setup/00_Runbook.md, Dashboard/DESIGN_LOG.md · to refresh, edit only the STATUS JSON blob in this file.</footer>

<div id="ov" onclick="closeDrawer()"></div>
<aside id="drawer">
  <button id="dx" onclick="closeDrawer()">✕</button>
  <div class="dk" id="d-kind"></div>
  <h3 id="d-title"></h3>
  <div class="dnote" id="d-note"></div>
  <div id="d-refs"></div>
</aside>

<script>
/* ============================================================
   STATUS MODEL — the only thing that changes between reviews.
   states: done | dark (built-but-dark) | spec | deferred
   ============================================================ */
const STATUS = {
  asOf: "2026-06-03",
  phases: [
    {id:"P0",  name:"Schema + Drive",      state:"done", note:"Master Sheet tab schemas + Drive folder structure. Done 2026-05-27.", refs:[["00_Architecture_and_Roadmap.md","Phased rollout table"]]},
    {id:"P1",  name:"Calendar + Sunday briefing", state:"dark", note:"sunday_briefing.py runs daily (mock output to Briefings/, email fallback). Calendar-Events tab live with manual rows. DARK because: Twilio not provisioned, Google Calendar connector not wired.", refs:[["00_Architecture_and_Roadmap.md","Phase 1 + Open blockers"],["Automation/README.md","cron table"]]},
    {id:"P2",  name:"Reminders engine",    state:"dark", note:"Engine built, dry-run works. DARK because: Twilio not provisioned. Definition-of-done boxes all unticked. Tombstone guard addendum is spec-only (~30-line edit).", refs:[["02_Reminders_Engine_Spec.md","Definition of done + Phase 6.1 addendum"]]},
    {id:"P3",  name:"Finance ingestion",   state:"spec", note:"Runbooks + drop-in code ready (bank scrapers, Gmail bill parser, Mindee). hebrew_categorizer + anomaly_detector are built-dark. Nothing ingests real money data yet except the manual CSV drop.", refs:[["Setup/01_Israeli_Bank_Scrapers.md","full runbook"],["Setup/03_Gmail_MCP_Bill_Parser.md",""]]},
    {id:"P4",  name:"Health + Education",  state:"dark", note:"Vaccine schedule + reminders seeds done. pediatric_milestones.py built (mock). Maccabi forwarder is runbook-only.", refs:[["Setup/09_Vaccine_Schedule_Seed.csv",""],["Automation/README.md","pediatric_milestones.py"]]},
    {id:"P5",  name:"Goals + coaching",    state:"dark", note:"4 goals seeded incl. Shanee's wellness goal. goal_coaching.py built (mock, Thu 19:00 cron suggested). Bright-line viz applied in dashboard prototype.", refs:[["Setup/10_Dashboard_Goals_Seed.csv",""],["02_Kickoff_Output_2026-05-30.md","goals + owners"]]},
    {id:"P6",  name:"Dashboard PWA",       state:"dark", note:"Prototype shipped 2026-05-28; design refresh 2026-05-30; full Hebrew chrome 2026-06-01. DARK because: demo data only — OAuth client not configured, GitHub Pages not set up.", refs:[["05_Dashboard_Design.md","recommended direction"],["Dashboard/DESIGN_LOG.md","session log"],["Dashboard/README_SETUP.md","OAuth setup"]]},
    {id:"P6.1",name:"Refinements (arc · ticker · queue)", state:"spec", note:"Schema columns (LastDoneBy, DoneAt, WriteQueue_Tombstone) spec'd. Neither the dashboard UI nor the engine tombstone guard is implemented — DESIGN_LOG marks it next-session work. CLAUDE.md snapshot overstates this phase.", refs:[["05_Dashboard_Design.md","Phase 6.1 schema bump"],["02_Reminders_Engine_Spec.md","Phase 6.1 addendum"]]}
  ],
  items: [
    {id:"1.1", t:"israeli-bank-scrapers cron",      state:"spec", week:"W1", deps:["bank"],    note:"Runbook + scrape.js drop-in ready. Biggest single ROI — kills the manual CSV drop.", refs:[["Setup/01_Israeli_Bank_Scrapers.md",""],["06_Lift_Recommendations_2026-05-30.md","§1.1"]]},
    {id:"1.2", t:"Hebcal — Shabbat + chagim",        state:"dark", week:"W1", deps:[],          note:"hebcal_client.py built, free API, cache works. Just needs wiring into the briefing run.", refs:[["Automation/README.md","hebcal_client.py"],["06_Lift_Recommendations_2026-05-30.md","§1.2"]]},
    {id:"1.3", t:"Yad2 + Madlan watchers",           state:"spec", week:"W3", deps:["apify"],   note:"Apify actors chosen, runbook ready. Gives the house goal a heartbeat.", refs:[["Setup/02_Yad2_Madlan_Watchers.md",""],["06_Lift_Recommendations_2026-05-30.md","§1.3"]]},
    {id:"1.4", t:"Dira BeHanacha tracker",           state:"dark", week:"W3", deps:[],          note:"dira_tracker.py built (stub fallback when site unreachable). Cron Wed 09:00 suggested.", refs:[["Automation/README.md","dira_tracker.py"],["06_Lift_Recommendations_2026-05-30.md","§1.4"]]},
    {id:"1.5", t:"Tipat Halav vaccine sync (kid-2)", state:"done", week:"—", deps:[],          note:"Manual monthly process + vaccine seed CSV in place. kid-2's 4-month shots are the next due.", refs:[["Setup/09_Vaccine_Schedule_Seed.csv",""],["06_Lift_Recommendations_2026-05-30.md","§1.5"]]},
    {id:"1.6", t:"Tofes 101 + miluim credits (Jan)",  state:"done", week:"Later", deps:[],      note:"One-shot January reminder seeded.", refs:[["Setup/08_Israeli_Reminders_Seed.csv",""],["06_Lift_Recommendations_2026-05-30.md","§1.6"]]},
    {id:"1.7", t:"Car-test SMS forward (TesTime)",    state:"spec", week:"—", deps:["fwd"],     note:"Forward-and-parse recipe documented; email forward not set up.", refs:[["06_Lift_Recommendations_2026-05-30.md","§1.7"]]},
    {id:"1.8", t:"Maccabi email forward + parse",     state:"spec", week:"—", deps:["gmail"],   note:"Runbook + maccabiParser.gs ready. No Maccabi API exists — forwarding is the play.", refs:[["Setup/07_Maccabi_Passport_Forwarders.md",""],["06_Lift_Recommendations_2026-05-30.md","§1.8"]]},
    {id:"1.9", t:"Passport trackers (kid-2 + adults)", state:"done", week:"—", deps:[],        note:"Recurring reminders seeded; Polish citizenship 30-day re-check included.", refs:[["Setup/07_Maccabi_Passport_Forwarders.md",""],["06_Lift_Recommendations_2026-05-30.md","§1.9"]]},
    {id:"1.10",t:"Hebrew/RTL chrome + EN toggle",     state:"done", week:"W4", deps:[],         note:"DONE incl. full Hebrew string set (2026-06-01, ahead of snapshot). lang/dir flip, t() walker, grammar-aware duePhrase, שפה toggle in Settings.", refs:[["Dashboard/DESIGN_LOG.md","2026-06-01 entries"],["06_Lift_Recommendations_2026-05-30.md","§1.10"]]},
    {id:"1.11",t:"Kol-Zchut guide_url per reminder",  state:"done", week:"—", deps:[],          note:"Reference links + column convention in place.", refs:[["Setup/11_Kol_Zchut_Reference_Links.md",""],["06_Lift_Recommendations_2026-05-30.md","§1.11"]]},
    {id:"1.12",t:"Inforu/Cellact SMS fallback",       state:"deferred", week:"—", deps:[],      note:"Deferred by Adar 2026-05-30. WhatsApp stays the only alert channel.", refs:[["06_Lift_Recommendations_2026-05-30.md","§1.12"]]},
    {id:"1.13",t:"Finanda open banking",              state:"deferred", week:"—", deps:[],      note:"Deferred — consumer eligibility unclear. Revisit Q4 2026.", refs:[["06_Lift_Recommendations_2026-05-30.md","§1.13"]]},
    {id:"2.1", t:"Gmail MCP bill parser",             state:"spec", week:"W2", deps:["gmail"],  note:"Runbook + billParser.gs ready. Contracts tab fills itself once deployed.", refs:[["Setup/03_Gmail_MCP_Bill_Parser.md",""],["06_Lift_Recommendations_2026-05-30.md","§2.1"]]},
    {id:"2.2", t:"Mindee receipt OCR",                state:"spec", week:"—", deps:["mindee"],  note:"Drive→Mindee→Sheets Zapier recipe documented; accounts not created.", refs:[["Setup/04_Mindee_Receipt_OCR.md",""],["06_Lift_Recommendations_2026-05-30.md","§2.2"]]},
    {id:"2.3", t:"iCloud → GCal public ICS",          state:"spec", week:"W1", deps:["ics"],    note:"15-minute runbook. Shanee shares the ICS URL → briefing sees both calendars.", refs:[["Setup/05_iCloud_GCal_Sync.md",""],["06_Lift_Recommendations_2026-05-30.md","§2.3"]]},
    {id:"2.4", t:"LlamaParse insurance PDFs",         state:"spec", week:"Later", deps:[],      note:"Approved, scheduled Later.", refs:[["06_Lift_Recommendations_2026-05-30.md","§2.4"]]},
    {id:"2.5", t:"Twilio → Meta Cloud API migration", state:"spec", week:"Later", deps:[],      note:"Approved; explicitly must not block anything else. <$5/mo difference at family volume.", refs:[["06_Lift_Recommendations_2026-05-30.md","§2.5"]]},
    {id:"2.6", t:"Voice capture — Siri → Sheet",      state:"spec", week:"W1", deps:["webhook"],note:"Runbook + webhookReceiver.gs ready. Wife-friendly capture.", refs:[["Setup/06_Voice_Receipt_Shortcuts.md",""],["06_Lift_Recommendations_2026-05-30.md","§2.6"]]},
    {id:"2.7", t:"PWA web-push",                      state:"deferred", week:"—", deps:[],      note:"Declined 2026-05-30 — WhatsApp stays the only alert channel.", refs:[["06_Lift_Recommendations_2026-05-30.md","§2.7"]]},
    {id:"2.8", t:"PDF/photo → calendar event (Ohai)", state:"dark", week:"—", deps:["apikey"],  note:"pdf_to_event.py built, multimodal. Most likely feature to win Shanee's daily use.", refs:[["Automation/README.md","pdf_to_event.py"],["06_Lift_Recommendations_2026-05-30.md","§2.8"]]},
    {id:"3.1-3.9", t:"Design lift — palette · type · cards · bright-line · empty states · icon · pill", state:"done", week:"W4", deps:[], note:"All nine applied to the dashboard prototype 2026-05-30 (week-4 work landed early). Warm-paper + indigo, Inter+Heebo+Geist Mono, big-number+sparkline drawers, Beeminder bright-line, 'Nothing on fire ☕'.", refs:[["Dashboard/DESIGN_LOG.md","2026-05-30 design refresh"],["06_Lift_Recommendations_2026-05-30.md","§3"]]},
    {id:"4.1", t:"Friday briefing (Claude reads Sheet)", state:"dark", week:"W2", deps:["twilio","apikey"], note:"friday_briefing.py built, template fallback without API key. Delivery dark until Twilio.", refs:[["Automation/README.md","friday_briefing.py"],["06_Lift_Recommendations_2026-05-30.md","§4.1"]]},
    {id:"4.2", t:"Hebrew merchant categorizer",       state:"dark", week:"W2", deps:["apikey"], note:"Built: cache → regex → LLM fallback. Works deterministically today; LLM tier needs key.", refs:[["Automation/README.md","hebrew_categorizer.py"],["06_Lift_Recommendations_2026-05-30.md","§4.2"]]},
    {id:"4.3", t:"Receipt photo → Sheet Shortcut",    state:"spec", week:"W1", deps:["webhook"],note:"Runbook ready, same webhook as 2.6.", refs:[["Setup/06_Voice_Receipt_Shortcuts.md",""],["06_Lift_Recommendations_2026-05-30.md","§4.3"]]},
    {id:"4.4", t:"Subscription creep + duplicates",   state:"dark", week:"—", deps:["feed"],    note:"anomaly_detector.py built; ran on sample CSV. Needs the real transactions feed (←1.1) to matter.", refs:[["Automation/README.md","anomaly_detector.py"],["06_Lift_Recommendations_2026-05-30.md","§4.4"]]},
    {id:"4.5", t:"Goal-coaching weekly retro",        state:"dark", week:"—", deps:[],          note:"goal_coaching.py built. Cron Thu 19:00 so Friday brief can pull it.", refs:[["Automation/README.md","goal_coaching.py"],["06_Lift_Recommendations_2026-05-30.md","§4.5"]]},
    {id:"4.6", t:"Pediatric milestone monitor",       state:"dark", week:"Later", deps:[],      note:"pediatric_milestones.py built (Tipat Halav schedule, non-scary by design).", refs:[["Automation/README.md","pediatric_milestones.py"],["06_Lift_Recommendations_2026-05-30.md","§4.6"]]},
    {id:"4.7", t:"Tasklet-style reply-to-act",        state:"spec", week:"Later", deps:["twilio"], note:"⚠ 06-doc marks this ✅ but no artifact exists (no script, no runbook) — corrected to approved-not-built in this review.", refs:[["06_Lift_Recommendations_2026-05-30.md","§4.7"]]},
    {id:"4.8", t:"Anomaly detection >2σ",             state:"dark", week:"—", deps:["feed"],    note:"Part of anomaly_detector.py (drift, spikes, merchant-suffix collapse). Same feed dependency as 4.4.", refs:[["Automation/README.md","anomaly_detector.py"],["06_Lift_Recommendations_2026-05-30.md","§4.8"]]},
    {id:"5.1", t:"WhatsApp group summarizer",         state:"dark", week:"—", deps:["baileys","twilio"], note:"Phases A–E built + mock-verified 2026-06-02 (groups: גן עידן, ועד הבית, שכונה, משפחה, סטודנטים). Tiered budget: critical_keywords bypass the 2/day cap. Phase F (accuracy tuning) not built. Open PO call: family-group criticals.", refs:[["07_WhatsApp_Group_Summarizer_Spec.md","build phases + open decisions"],["Automation/README.md","whatsapp_summarizer.py"]]},
    {id:"6.1g",t:"Shanee's wellness goal (Goal 4)",   state:"done", week:"—", deps:[],          note:"Seeded; tracked weekly by goal_coaching.py. Makes the Goals tab a shared system.", refs:[["Setup/10_Dashboard_Goals_Seed.csv",""],["06_Lift_Recommendations_2026-05-30.md","§6.1"]]}
  ],
  deps: [
    {id:"twilio", label:"Twilio WhatsApp sender", effort:"~1 evening", key:true, unlocks:["Reminders engine live","Sunday briefing delivery","Friday briefing delivery (4.1)","Summarizer alert dispatch (5.1)","Reply-to-act (4.7, later)"], note:"THE dominant node — one credential unblocks 5 lanes. Engine + summarizer share a single send-function swap point.", refs:[["02_Reminders_Engine_Spec.md","Definition of done"],["07_WhatsApp_Group_Summarizer_Spec.md","Built 2026-06-02 note"]]},
    {id:"baileys",label:"Baileys QR pairing",      effort:"~30 min once", unlocks:["WhatsApp group ingestion (5.1)"], note:"Bridge code is built and heartbeat-monitored; needs one QR scan on an always-on machine.", refs:[["07_WhatsApp_Group_Summarizer_Spec.md","Provider choice"]]},
    {id:"gcal",  label:"Google Calendar connector", effort:"~30 min", unlocks:["Real calendar in briefings + dashboard"], note:"Suggested since Phase 1; manual Calendar-Events rows are the interim.", refs:[["00_Architecture_and_Roadmap.md","Open blockers"]]},
    {id:"gmail", label:"Gmail Apps Script deploy",  effort:"~1.5h", unlocks:["Bill parser (2.1)","Maccabi forwarder (1.8)"], note:"One Apps Script deployment covers both parsers.", refs:[["Setup/03_Gmail_MCP_Bill_Parser.md",""]]},
    {id:"webhook",label:"Apps Script webhook + Shortcuts", effort:"~1.5h", unlocks:["Voice capture (2.6)","Receipt photo (4.3)"], note:"webhookReceiver.gs + two iOS Shortcuts.", refs:[["Setup/06_Voice_Receipt_Shortcuts.md",""]]},
    {id:"oauth", label:"Dashboard OAuth client",    effort:"~15 min", unlocks:["Dashboard reads/writes real Sheet"], note:"5-minute Google Cloud setup documented in the Dashboard README.", refs:[["Dashboard/README_SETUP.md",""]]},
    {id:"ghpages",label:"GitHub Pages + folder sync", effort:"~1h once", unlocks:["Phone-preview workflow","Shanee's solo design sessions"], note:"Process unblock, not product. 6-box checklist in CLAUDE.md, all unticked, Adar's responsibility.", refs:[["CLAUDE.md","DevOps one-time setup checklist"]]},
    {id:"bank",  label:"Bank credentials + VPS",    effort:"~2h", unlocks:["Bank scrapers (1.1)","Real anomaly detection (4.4/4.8)"], note:"Biggest lift, biggest payoff per the runbook sequence.", refs:[["Setup/01_Israeli_Bank_Scrapers.md",""]]},
    {id:"apikey",label:"ANTHROPIC_API_KEY",         effort:"~5 min", unlocks:["LLM tier: categorizer · summarizer · briefings · pdf_to_event"], note:"Everything has a deterministic fallback — this upgrades quality, doesn't gate function.", refs:[["Automation/README.md","Env vars"]]},
    {id:"apify", label:"Apify account",             effort:"~1h", unlocks:["Yad2/Madlan watchers (1.3)"], note:"", refs:[["Setup/02_Yad2_Madlan_Watchers.md",""]]},
    {id:"ics",   label:"iCloud ICS share (Shanee)", effort:"~15 min", unlocks:["Both calendars in briefing (2.3)"], note:"Shanee shares once; Adar subscribes once.", refs:[["Setup/05_iCloud_GCal_Sync.md",""]]},
    {id:"mindee",label:"Mindee + Zapier accounts",  effort:"~45 min", unlocks:["Receipt OCR (2.2)"], note:"", refs:[["Setup/04_Mindee_Receipt_OCR.md",""]]},
    {id:"fwd",   label:"Email forwards (car/license)", effort:"~30 min", unlocks:["Car-test parser (1.7)"], note:"", refs:[["06_Lift_Recommendations_2026-05-30.md","§1.7"]]},
    {id:"feed",  label:"(← bank creds) transactions feed", effort:"derived", unlocks:["Anomaly detectors get real data"], note:"Not a credential itself — flows from the bank-scraper unlock.", refs:[["Setup/01_Israeli_Bank_Scrapers.md",""]]}
  ],
  lanes: [
    {id:"briefings", name:"Briefings (Sunday + Friday + daily)", built:85, live:30, note:"Scripts run daily and write real files to Briefings/ — that's the 30%. Delivery to WhatsApp is the missing 70% (Twilio). Hebcal built, not yet wired in.", refs:[["Automation/README.md","cron table"]]},
    {id:"reminders", name:"Reminders engine", built:80, live:20, note:"Dry-run daily with log. Missing: Twilio dispatch, tombstone guard (spec-only), 20+ real seed rows, end-to-end done-cycle. All 6 DoD boxes unticked.", refs:[["02_Reminders_Engine_Spec.md","Definition of done"]]},
    {id:"whatsapp",  name:"WhatsApp summarizer", built:85, live:0, note:"A–E of six phases built and mock-verified. 0% live: bridge unpaired, alerts mock. Two steps to live: QR scan + Twilio swap.", refs:[["07_WhatsApp_Group_Summarizer_Spec.md","Build phases"]]},
    {id:"finance",   name:"Finance ingestion", built:30, live:5, note:"Categorizer + anomaly scripts built-dark; ingestion is runbook-only. The 5% is the manual CSV drop — the system's only real data input today.", refs:[["Setup/01_Israeli_Bank_Scrapers.md",""]]},
    {id:"dashboard", name:"Dashboard PWA", built:70, live:0, note:"Prototype complete with Hebrew chrome; demo data only. Missing: OAuth (15 min), GH Pages (1h), 6.1 UI (arc/ticker/queue — spec'd, next session).", refs:[["Dashboard/DESIGN_LOG.md",""],["05_Dashboard_Design.md",""]]},
    {id:"health",    name:"Health + Education", built:60, live:10, note:"Seeds importable today (the 10%). Milestone script built-dark; Maccabi forwarder runbook-only.", refs:[["Setup/09_Vaccine_Schedule_Seed.csv",""]]},
    {id:"goals",     name:"Goals + coaching", built:70, live:10, note:"Goals seeded incl. Shanee's wellness (the 10%). Coaching script built-dark; bright-line viz in prototype.", refs:[["Setup/10_Dashboard_Goals_Seed.csv",""]]}
  ],
  costs: {
    today: {total:0, lines:[["Everything in mock mode — no paid keys wired",0]]},
    full:  {total:70, lines:[["WhatsApp classification (Claude Haiku)",33],["Twilio WhatsApp",18],["VPS for bank scrapers (estimate — unpriced in docs)",18],["Friday briefing LLM",1.5]]},
    oneTime: {label:"Dedicated Android for Baileys bridge", ils:400},
    declined:[["Whapi.cloud","₪140/mo"],["Superhuman / Shortwave","₪110/mo each"],["CalendarBridge","₪18/mo"]],
    note:"Today = ₪0/mo: every script runs its free deterministic fallback. Full operation ≈ ₪70/mo (~$20). Declined spend kept the number low: Baileys over Whapi alone saves ₪140/mo.",
    refs:[["07_WhatsApp_Group_Summarizer_Spec.md","Cost ceiling"],["06_Lift_Recommendations_2026-05-30.md","§2.5 + What to skip"]]
  }
};

/* ============================ render ============================ */
const $=q=>document.querySelector(q);
const stateName={done:"DONE",dark:"BUILT-DARK",spec:"SPEC ONLY",deferred:"DEFERRED"};
document.getElementById("asof").textContent=STATUS.asOf;

/* drawer */
function openDrawer(kind,title,note,refs){
  $("#d-kind").textContent=kind;
  $("#d-title").textContent=title;
  $("#d-note").textContent=note||"";
  $("#d-refs").innerHTML=(refs||[]).map(r=>`<div class="dref"><b>${r[0]}</b>${r[1]?` — ${r[1]}`:""}</div>`).join("");
  $("#ov").classList.add("open"); $("#drawer").classList.add("open");
}
function closeDrawer(){ $("#ov").classList.remove("open"); $("#drawer").classList.remove("open"); }
document.addEventListener("keydown",e=>{if(e.key==="Escape")closeDrawer();});

/* timeline */
$("#timeline").innerHTML=STATUS.phases.map((p,i)=>`
  <div class="phase ${p.state}" data-i="${i}">
    <div class="pnum">${p.id}</div>
    <div class="pname">${p.name}</div>
    <div class="pstate">${stateName[p.state]}</div>
  </div>`).join("");
$("#timeline").addEventListener("click",e=>{
  const el=e.target.closest(".phase"); if(!el)return;
  const p=STATUS.phases[+el.dataset.i];
  openDrawer("PHASE · "+p.id,p.name,`${stateName[p.state]}\n\n${p.note}`,p.refs);
});

/* board + filters */
let fState="all", fWeek="all";
const weeks=["all","W1","W2","W3","W4","Later","—"];
const states=["all","done","dark","spec","deferred"];
function chips(){
  $("#filters").innerHTML=
    states.map(s=>`<span class="chip ${fState===s?"on":""}" data-k="s" data-v="${s}">${s==="all"?"all states":stateName[s]||s}</span>`).join("")+
    `<span style="width:10px"></span>`+
    weeks.map(w=>`<span class="chip ${fWeek===w?"on":""}" data-k="w" data-v="${w}">${w==="all"?"all weeks":w==="—"?"unscheduled":w}</span>`).join("");
}
function board(){
  const list=STATUS.items.filter(it=>(fState==="all"||it.state===fState)&&(fWeek==="all"||it.week===fWeek));
  $("#board").innerHTML=list.map((it)=>`
    <div class="card ${it.state}" data-id="${it.id}">
      <div class="cid">${it.id}</div>
      <div class="ct">${it.t}</div>
      <div class="cm"><span class="badge b-${it.state}">${stateName[it.state]}</span>${it.week!=="—"?`<span class="badge b-week">${it.week}</span>`:""}</div>
    </div>`).join("")||`<div style="color:var(--muted);font-size:13px">No items match this filter.</div>`;
}
chips();board();
$("#filters").addEventListener("click",e=>{
  const c=e.target.closest(".chip");if(!c)return;
  if(c.dataset.k==="s")fState=c.dataset.v; else fWeek=c.dataset.v;
  chips();board();
});
$("#board").addEventListener("click",e=>{
  const el=e.target.closest(".card");if(!el)return;
  const it=STATUS.items.find(x=>x.id===el.dataset.id);
  const dep=it.deps.length?`\n\nBlocked by: ${it.deps.map(d=>{const dd=STATUS.deps.find(x=>x.id===d);return dd?dd.label:d;}).join(" · ")}`:"\n\nNo blockers.";
  openDrawer("BACKLOG ITEM · "+it.id,it.t,`${stateName[it.state]}${it.week!=="—"?" · ship "+it.week:""}${dep}\n\n${it.note}`,it.refs);
});

/* dependency graph */
const credEls={}, unlockEls=[];
STATUS.deps.forEach(d=>{
  if(d.id==="feed")return; // derived node lives in notes, not the graph
  const el=document.createElement("div");
  el.className="dep"+(d.key?" keynode":"");
  el.innerHTML=`<b>${d.label}</b><div class="de">${d.effort} → unlocks ${d.unlocks.length}</div>`;
  el.onclick=()=>{highlight(d.id);openDrawer("CREDENTIAL / SETUP",d.label,`Effort: ${d.effort}\n\nUnlocks:\n${d.unlocks.map(u=>"• "+u).join("\n")}\n\n${d.note||""}`,d.refs);};
  $("#credcol").appendChild(el); credEls[d.id]=el;
  d.unlocks.forEach(u=>{
    const ue=document.createElement("div"); ue.className="unlock"; ue.textContent=u; ue.dataset.dep=d.id;
    ue.onclick=()=>{highlight(d.id);};
    $("#unlockcol").appendChild(ue); unlockEls.push(ue);
  });
});
function wires(hl){
  const svg=$("#wires"), wrap=$("#depwrap"), wr=wrap.getBoundingClientRect();
  svg.setAttribute("viewBox",`0 0 ${wr.width} ${wr.height}`);
  let html="";
  unlockEls.forEach(ue=>{
    const ce=credEls[ue.dataset.dep]; if(!ce)return;
    const a=ce.getBoundingClientRect(), b=ue.getBoundingClientRect();
    const x1=a.right-wr.left, y1=a.top+a.height/2-wr.top, x2=b.left-wr.left, y2=b.top+b.height/2-wr.top;
    const mx=(x1+x2)/2;
    html+=`<path class="${hl===ue.dataset.dep?"hl":""}" d="M${x1},${y1} C${mx},${y1} ${mx},${y2} ${x2},${y2}"/>`;
  });
  svg.innerHTML=html;
}
function highlight(id){
  Object.entries(credEls).forEach(([k,el])=>el.classList.toggle("hl",k===id));
  unlockEls.forEach(ue=>ue.classList.toggle("hl",ue.dataset.dep===id));
  wires(id);
}
window.addEventListener("resize",()=>wires());
requestAnimationFrame(()=>wires());

/* lanes */
$("#lanes").innerHTML=STATUS.lanes.map((l,i)=>`
  <div class="lane" data-i="${i}">
    <div class="ln"><span>${l.name}</span><span>built ${l.built}% · live ${l.live}%</span></div>
    <div class="bar built"><i style="width:${l.built}%"></i></div>
    <div class="bar live"><i style="width:${l.live}%"></i></div>
    <div class="barlab"><span>▮ built</span><span>▮ actually running</span></div>
  </div>`).join("");
$("#lanes").addEventListener("click",e=>{
  const el=e.target.closest(".lane");if(!el)return;
  const l=STATUS.lanes[+el.dataset.i];
  openDrawer("LANE",l.name,`Built ${l.built}% · actually running ${l.live}%\n\n${l.note}`,l.refs);
});

/* costs */
const C=STATUS.costs;
$("#costs").innerHTML=`
  <div class="costcol" id="c-today"><h3>Today</h3><div class="big">₪${C.today.total}<span style="font-size:13px;color:var(--muted)">/mo</span></div>
    ${C.today.lines.map(l=>`<div class="costline"><span>${l[0]}</span><b>₪${l[1]}</b></div>`).join("")}</div>
  <div class="costcol" id="c-full"><h3>At full operation</h3><div class="big">~₪${C.full.total}<span style="font-size:13px;color:var(--muted)">/mo</span></div>
    ${C.full.lines.map(l=>`<div class="costline"><span>${l[0]}</span><b>₪${l[1]}</b></div>`).join("")}
    <div class="costline" style="margin-top:8px;border-top:1px solid var(--line);padding-top:6px"><span>One-time: ${C.oneTime.label}</span><b>₪${C.oneTime.ils}</b></div></div>`;
$("#costs").addEventListener("click",()=>openDrawer("COSTS","₪/month — today vs full operation",
  C.note+"\n\nDeclined spend:\n"+C.declined.map(d=>`• ${d[0]} (${d[1]})`).join("\n"),C.refs));
</script>
</body>
</html>

=== End: Progress/index.html ===

=== File: Briefings/gemini_deck_prompt_2026-06-03.md ===
# Gemini PRO prompt — Progress Review Deck (2026-06-03)

*Copy everything between the fences into a fresh Gemini PRO chat, attach the files listed at the bottom, run.*

```
You are generating a slide deck for a progress review of "Family Inc", a household-
automation project. The audience is the two product owners (Adar = CTO, Shanee =
Chief Design). Tone: honest, calm, zero hype. This is an internal "where are we,
where are we going" review — not a pitch. Nothing is being sold.

## Project one-paragraph context
Adar + Shanee's household-automation system. Master DB = Google Sheets (Family_OS).
PWA dashboard pinned to iPhone with write-back to the Sheet. Briefings via WhatsApp
(Twilio). Israeli context (ILS, Hebrew, Maccabi healthcare). Family = 2 adults +
2 kids (kid-1 3yr, kid-2 3mo). Operating principles: briefings > notifications,
alert budget 2/day, no kid-facing UI, boring tech, one source of truth per domain.

## The status data (as of 2026-06-03 — use these numbers, do not invent)

HEADLINE: nothing is live yet. Substantial code exists; all of it runs in mock/dry
mode. Three states apply everywhere: DONE (live/visible), BUILT-DARK (code works,
awaits a credential or one-time setup), SPEC-ONLY (runbook/spec written, nothing built).

### Phases
| Phase | What | State |
|---|---|---|
| 0 | Sheet schema + Drive structure | DONE (2026-05-27) |
| 1 | Calendar merge + Sunday briefing | BUILT-DARK — script runs daily, writes to files/email fallback; Google Calendar connector not wired; Twilio not provisioned |
| 2 | Reminders engine | BUILT-DARK — dry-run works; Twilio not provisioned; tombstone race-guard addendum SPEC-ONLY (~30-line edit pending) |
| 3 | Finance ingestion | SPEC-ONLY — runbooks + drop-in scripts ready (bank scrapers, Gmail bill parser, Mindee OCR, Hebrew categorizer is BUILT-DARK) |
| 4 | Health/education trackers | seeds DONE (vaccine schedule, reminders CSVs); pediatric milestone script BUILT-DARK |
| 5 | Goals + coaching | seeds DONE (incl. Shanee's wellness goal); goal-coaching script BUILT-DARK |
| 6 | Dashboard PWA | BUILT-DARK — prototype shipped, demo mode, full Hebrew chrome (done 2026-06-01); live Sheets OAuth not configured; GitHub Pages hosting not set up |
| 6.1 | Dashboard refinements (7-day arc, appreciation ticker, offline queue+tombstone) | SPEC-ONLY — schema columns defined, UI + engine work pending |
| — | WhatsApp group summarizer | BUILT-DARK — phases A–E built + mock-verified 2026-06-02; awaits one-time Baileys QR pairing + Twilio swap; Phase F (accuracy tuning) not built |

### Built-but-dark inventory (10 scripts, all run today in mock mode)
sunday_briefing, reminders_engine (dry-run), friday_briefing, hebcal_client,
anomaly_detector, pediatric_milestones, goal_coaching, hebrew_categorizer,
pdf_to_event, whatsapp_summarizer (+ Baileys bridge listener, Node).

### What unblocks what (exact dependency → items freed)
| Credential / setup | Effort | Unblocks |
|---|---|---|
| Twilio WhatsApp sender | ~1 evening | 5 lanes: reminders engine live, Sunday briefing delivery, Friday briefing delivery, WhatsApp summarizer alerts, future reply-to-act |
| Baileys QR pairing on always-on machine | ~30 min, one-time | WhatsApp group summarizer ingestion (already built) |
| Google Calendar connector | ~30 min | real calendar data in briefings + dashboard |
| Gmail Apps Script deploy | ~1.5h | bill parser, Maccabi forwarder, voice/receipt webhooks |
| Dashboard OAuth client (Google Cloud) | ~15 min | dashboard reads/writes real Sheet |
| GitHub Pages + folder sync | ~1h, one-time | phone-preview workflow; Shanee's solo design sessions |
| Bank credentials + small VPS | ~2h | bank scrapers → automatic transactions feed → anomaly detector gets real data |
| ANTHROPIC_API_KEY in env | ~5 min | LLM tier of categorizer/summarizer/briefings (all have free deterministic fallbacks) |
| Apify account | ~1h | Yad2/Madlan real-estate watchers |

### Ship order ahead (from the live backlog, 06 doc)
Week 1: bank scrapers, Hebcal wire-in, iCloud→GCal ICS, voice+receipt Shortcuts.
Week 2: Gmail bill parser, Hebrew categorizer live, Friday briefing live.
Week 3: Yad2/Madlan + Dira trackers, bright-line goal viz.
Week 4 (design): DONE early — palette/type/RTL already applied.
Later: LlamaParse, reply-to-act, Meta API migration, milestone monitor.

### Costs
Today: ₪0/month (everything mock, no paid keys wired).
Full operation: ~$9/mo WhatsApp classification (Claude Haiku) + ~$0.40/mo Friday
briefing + <$5/mo Twilio + ~$5/mo VPS (estimate, unpriced in docs) ≈ $20/mo ≈
₪70/mo. One-time: ~₪400 dedicated Android for the WhatsApp bridge.
Declined to spend on: Whapi ($39/mo), Superhuman/Shortwave ($30/mo each), Plaid-class
enrichment, CalendarBridge ($5/mo).

### The 3 decisions the POs need to make
1. Provision Twilio now? (single highest-leverage unlock: 5 lanes, <$5/mo)
2. Schedule the one-time devops setup (GH Pages + folder sync + OAuth)? It blocks
   Shanee's ability to run solo design sessions.
3. Family-group criticals: should an emergency keyword in the digest-only family
   group override its "never alert" routing and actually ping? (Open joint-PO call
   from the WhatsApp spec, surfaced 2026-06-02.)

## Your task
Produce a slide deck (Google Slides export or equivalent) following this exact
narrative arc, one section per slide unless noted. VISUAL-FIRST: max 15 words of
prose per slide; the visual carries the message.

| # | Slide | One-line message | Required visual |
|---|---|---|---|
| 1 | What we set out to build | "A calm household OS: one Sheet, briefings not notifications." | L0 system sketch: inputs → Sheet → automation → WhatsApp/PWA |
| 2 | What exists today, demoable | "The skeleton is real: schema, prototype dashboard, 10 working scripts." | Status grid of phases 0–6.1, three-state color coding (done / built-dark / spec-only) |
| 3 | Built-but-dark | "Code is not the bottleneck — credentials are." | Dependency graph: 9 credential nodes → the items each unblocks; Twilio node visually dominant |
| 4 | The ship order ahead | "Four weeks of unblocking, in leverage order." | Timeline/swimlane: weeks 1–4 + Later, items as bars |
| 5 | Costs | "₪0 today → ~₪70/month at full operation." | Cost bars: today vs full, itemized stack; one-time ₪400 marked separately |
| 6 | Three decisions | "Twilio? Devops day? Family-group emergencies?" | Decision cards, one per call, each with the tradeoff in one line |

## What NOT to do
- Do not invent numbers, features, or status claims not in the data above.
- Do not soften "nothing is live yet" — the POs want the honest picture.
- Do not call built-but-dark items "done." Three states minimum everywhere.
- No hype language ("amazing progress", "incredible"), no emojis, no stock photos.
- Do not add slides (no team slide, no roadmap-beyond-data slide, no thank-you slide).

## Required output
The 6 slides above, each with: title, the one-line message, the visual (rendered,
not described), and presenter notes of max 3 sentences pulling only from the data.
```

**Attach these files alongside the prompt:**

- `06_Lift_Recommendations_2026-05-30.md` — backlog + ship order the data came from
- `00_Architecture_and_Roadmap.md` — phases + system sketch
- `CLAUDE.md` — principles + file map
- `07_WhatsApp_Group_Summarizer_Spec.md` — build-status table + open PO decision
- `Automation/README.md` — script inventory

*Status data assembled by Claude (Lead Architect) 2026-06-03 from the docs above; verified against DESIGN_LOG.md and Setup/00_Runbook.md. Known corrections to stale snapshots: Hebrew chrome strings are DONE (not pending); item 4.7 is approved-not-built (06 doc's ✅ overstates); Phase 6.1 dashboard UI is spec-only.*

=== End: Briefings/gemini_deck_prompt_2026-06-03.md ===

=== File: Briefings/gemini_architecture_prompt_2026-06-03.md ===
# Gemini PRO prompt — Architecture Overview + Deep Dive (2026-06-03)

*Copy everything between the fences into a fresh Gemini PRO chat, attach the files listed at the bottom, run.*

```
You are writing the canonical architecture document for "Family Inc", a household-
automation project. Readers: the two product owners (Adar = CTO, technical;
Shanee = Chief Design, product-technical). Purpose: a layered-zoom reference that
answers "how does this system work?" at three depths without the reader opening
code. Tone: precise, dry, no hype.

## Project one-paragraph context
Adar + Shanee's household-automation system. Master DB = Google Sheets (Family_OS,
one tab per domain). PWA dashboard (vanilla HTML/JS, no build step) pinned to
iPhone, write-back to the Sheet. Briefings + alerts via WhatsApp (Twilio — not yet
provisioned). Python automation scripts + one Node WhatsApp bridge. Israeli context
(ILS, Hebrew strings, Maccabi). Principles: one source of truth per domain, boring
tech, alert budget 2/day, briefings > notifications.

## CRITICAL honesty requirement — the "not built" overlay
As of 2026-06-03 NOTHING IS LIVE. All scripts run in mock/dry mode; the dashboard
runs on demo data; Twilio, Google Calendar connector, OAuth, GitHub Pages, and the
Baileys pairing are all pending. Every diagram you draw MUST visually distinguish
three states:
- SOLID (exists and is exercised today, e.g. mock runs writing to Briefings/)
- DASHED (built-but-dark: code complete, awaits a credential/one-time setup)
- DOTTED + label "spec only" (designed, not built: tombstone engine guard,
  6.1 dashboard UI, Gmail bill parser, bank scrapers, reply-to-act)
In Mermaid, use solid/dashed link styles and a legend node in every diagram.
A diagram that shows the system as if it were running is a failure.

## Required structure

### L0 — One-screen system overview
Single Mermaid flowchart: HUMAN INPUTS (calendars, CSV drops, manual Sheet entries,
Gmail, WhatsApp groups) → SOURCE OF TRUTH (Drive folder + Family_OS Sheet, tabs
listed) → AUTOMATION LAYER (daily 07:30 reminders run, hourly WhatsApp classify,
Fri 11:00 + Sun 18:00 briefings, monthly digest) → OUTPUTS (WhatsApp via Twilio,
PWA dashboard read+write-back). Apply the three-state overlay. Max one screen.

### L1 — Per-lane deep dives (one subsection + one Mermaid diagram each)
1. Briefings lane: sunday_briefing.py + friday_briefing.py + hebcal_client +
   goal_coaching feeding in; email fallback today, Twilio when provisioned.
2. Reminders lane: Reminders tab as keystone; lead-time tiers; 2/day budget with
   coalescing + priority trimming; quiet hours; recurrence bumping; the planned
   tombstone guard (mark spec-only).
3. Finance ingestion lane: today manual CSV drop (the only live-ish input) →
   planned bank scrapers on VPS, Gmail bill parser, Mindee OCR, hebrew_categorizer
   (cache → regex → LLM). Mark planned parts dashed/dotted honestly.
4. WhatsApp summarizer lane: Baileys bridge (JSONL inbox, heartbeat file) → hourly
   classifier (Haiku + 5 hard rules + deterministic fallback) → per-group routing →
   tiered budget (critical_keywords bypass) → daily digest + CSV staging (gspread
   pending). Phases A–E built dark; F not built.
5. Dashboard write-back lane: GIS OAuth → gapi batchGet → optimistic UI →
   offline queue in localStorage → flush with WriteQueue_Tombstone → engine skip
   window 6h. UI for arc/ticker/queue is spec-only — say so.

### L2 — Data contracts
- Family_OS tab schemas: Reminders (cols A–O incl. LastDoneBy, DoneAt,
  WriteQueue_Tombstone), WhatsApp_Inbox, WhatsApp_Archive, WhatsApp_Group_Config,
  Goals, Finance tabs. Render as tables, note which columns exist vs Phase-6.1
  pending.
- Tombstone semantics: written on every dashboard write (incl. queued flush);
  engine skips rows tombstoned <6h; residual race when phone offline >6h
  (one extra alert, accepted); future-tombstone clock-skew rule.
- Alert budget tiers: 2/day hard cap per recipient; coalescing first; priority
  trim order; critical_keywords safety tier bypasses the cap; family-group
  criticals float to digest "NEEDS A LOOK" (never ping — open PO decision).
- Mock-mode contract: every script announces RUNNING IN MOCK MODE, deterministic
  fallbacks when ANTHROPIC_API_KEY absent, single Twilio swap point
  (dispatch_alert / send function) shared by engine + summarizer.

### Data-flow walkthrough — one real day
Narrate Wednesday: 05:30 anomaly scan → 07:30 reminders run (tombstone check,
budget, digest render) → 07:30 daily digest incl. WhatsApp groups section →
hourly classify runs (22:14 daycare "מחר" message → hard rule 2 → ALERT within
the hour, budget permitting) → Thu 19:00 goal retro → Fri 11:00 Friday briefing
(Shabbat times via Hebcal) → Sat 21:00 Sunday review. At each step state what
ACTUALLY happens today (mock output to Briefings/) vs at full operation
(WhatsApp message arrives). Two parallel columns or paired paragraphs.

### Closing section — "What is NOT built" (explicit list)
Tombstone engine guard, 6.1 dashboard UI (arc/ticker/offline queue), Twilio
dispatch, GCal connector, OAuth wiring, GH Pages, Baileys pairing, gspread Sheet
writes, bank scrapers, Gmail bill parser, Mindee, Apify watchers, reply-to-act,
Phase F tuning. One line each: what it is + what unblocks it.

## What NOT to do
- No invented components, schedules, or column names — everything is in the
  attached docs.
- No "future vision" sections beyond the explicit not-built list.
- No style/UX commentary — this is an architecture doc, not a design review.
- Do not collapse the three states into done/not-done. That distinction is the
  point of the document.

## Required output format
Markdown document, headings exactly: L0 / L1 (5 subsections) / L2 / A Day in the
Life / What Is NOT Built. Mermaid code blocks at every level (L0: 1, L1: 5,
L2: at least 1 for tombstone timing). Each Mermaid block must include a legend.
Be terse — diagrams carry the weight.
```

**Attach these files alongside the prompt:**

- `00_Architecture_and_Roadmap.md` — system sketch + phases
- `CLAUDE.md` — principles, roles, file map
- `02_Reminders_Engine_Spec.md` — reminders + tombstone contract
- `05_Dashboard_Design.md` — dashboard + write-back + offline queue contract
- `07_WhatsApp_Group_Summarizer_Spec.md` — summarizer lanes + tiered budget
- `Automation/README.md` — script inventory + cron schedule

*Assembled by Claude (Lead Architect) 2026-06-03. The "not built" overlay reflects the verified status model of this date — see `Briefings/gemini_deck_prompt_2026-06-03.md` for the full status table.*

=== End: Briefings/gemini_architecture_prompt_2026-06-03.md ===

=== File: 00_Architecture_and_Roadmap.md ===
# Family inc. — Architecture & Roadmap

*Living design doc. Edit freely; Cowork will update as decisions evolve.*

Last updated: 2026-05-27

## North-Star goals

1. Help the family set and achieve long-term (3–5 year) goals.
2. Use AI to enhance Health, Finance, Education, and other life areas.
3. Maximum automation — minimum daily babysitting of the system.
4. Provide a simple "today / this week" dashboard accessible on iPhone (web first, native later if it earns it).

## Operating principles

- **One source of truth per domain.** No data lives in two places. If it does, one of them is a view.
- **Boring tech where possible.** Google Sheets > custom database. CSV drops > brittle scrapers. PWA > native app — until proven otherwise.
- **Alerts are a budget, not a firehose.** Better one well-timed WhatsApp at 8 AM than ten throughout the day. Hard cap to be enforced.
- **Briefings, not notifications.** Daily 1-line digest, weekly multi-domain briefing, monthly deep dive. Real-time alerts reserved for true urgency (overdue car test, doctor cancellation, etc.).
- **Partner-readable, kid-aware.** Both adults can read everything; kids' data is structured but adult-mediated.

## Foundational decisions (2026-05-25)

| Decision | Choice | Reason |
|---|---|---|
| Alert channel | WhatsApp via Twilio | Reliable, real API, family already on WhatsApp |
| Finance ingestion | Manual monthly CSV drop into Drive | Israeli banks lack APIs; scrapers are fragile and credential-risky |
| Storage | Google Drive + Google Sheets | Easiest automation surface; both adults already have Google accounts |
| Audience | Adar + partner | Shared access; no kid-facing UI yet |
| Dashboard | Web (PWA) first; native iOS later if needed | 10x faster to build; pin to home screen feels native enough |

## System architecture

```
+--------------------------------------------------------------+
|  HUMAN INPUTS                                                |
|  - Google Calendar + iCloud Calendar (life events)           |
|  - Bank/card CSVs dropped into Drive (monthly)               |
|  - Manual entries in the master Sheet (goals, contacts, etc.)|
|  - Gmail (incoming bills, school newsletters)                |
+----------------------+---------------------------------------+
                       |
                       v
+--------------------------------------------------------------+
|  SOURCE OF TRUTH                                             |
|  Google Drive folder: Family inc./                           |
|   |-- Family_OS.gsheet  <- master DB (tabs per domain)       |
|   |-- /Finance_CSVs/    <- monthly bank/card exports         |
|   |-- /Documents/       <- contracts, insurance, school docs |
|   +-- /Briefings/       <- generated reports                 |
+----------------------+---------------------------------------+
                       |
                       v
+--------------------------------------------------------------+
|  AUTOMATION LAYER (Cowork scheduled tasks)                   |
|  - Daily 07:30: today's-events check, urgent reminders fire  |
|  - Weekly Sun 18:00: full family briefing generated          |
|  - Monthly 1st: finance digest + anomaly report              |
|  - On CSV upload: parse, categorize, write back to Sheet     |
+----------------------+---------------------------------------+
                       |
        +--------------+--------------+
        v                             v
+------------------+         +----------------------+
|  ALERTS          |         |  DASHBOARD (PWA)     |
|  WhatsApp via    |         |  Pinned to iPhone    |
|  Twilio          |         |  Read-only view      |
|  (Adar + partner)|         |  of the Sheet        |
+------------------+         +----------------------+
```

## Master Sheet schema (draft)

Tabs in `Family_OS.gsheet`:

1. **People** — name, role, DOB, ID number (Teudat Zehut), Kupat Holim, blood type, allergies.
2. **Calendars** — registered calendar sources, sync status, color coding.
3. **Reminders** — universal table: title, domain, owner, due date, lead times (60/30/7/1), status, last sent, channel.
4. **Finance — Accounts** — bank/card accounts, last imported date, balance snapshot.
5. **Finance — Transactions** — append-only ledger from CSV imports; columns: date, account, description, amount, category, tags, notes.
6. **Finance — Budget** — monthly category targets vs actuals.
7. **Goals** — long-term goal, owner, target date, 90-day milestone, % complete, last update.
8. **Education** — child, school/preschool, year, key dates (registry windows, parent-teacher), contacts.
9. **Health** — person, provider, last visit, next due, notes; sub-tab for vaccine schedule.
10. **Car** — vehicle, plate, test date, insurance renewal, license expiry, mechanic.
11. **Contacts** — doctors, teachers, mechanic, plumber, lawyer, accountant — tagged by domain.
12. **Contracts** — insurance, mortgage, utilities — renewal dates, premiums, providers.

The **Reminders** tab is the keystone. Every domain writes to it, and one daily automation reads it.

## Phased rollout

| Phase | Deliverable | Status |
|---|---|---|
| 0 | Master Sheet created with all tab schemas; Drive folder structure; this doc | **done (2026-05-27)** |
| 1 | Calendar merge + Sunday-evening briefing piped to WhatsApp | **in progress** — `sunday_briefing.py` built, `Calendar-Events` tab added, scheduled Sunday 18:00 with email fallback; Google Calendar connector suggested but not yet connected |
| 2 | Reminders engine: daily check, escalation tiers, WhatsApp delivery | **in progress** — spec done (`02_Reminders_Engine_Spec.md`), dry-run engine built, Twilio not yet wired |
| 3 | Finance ingestion + monthly digest | not started |
| 4 | Education + Health trackers wired into Reminders | not started |
| 5 | Long-term goals tracking + Friday progress report | not started |
| 6 | Dashboard PWA pinned to iPhone | not started |

## Assumptions in force (2026-05-27)

- **Partner sign-off assumed.** `03_Partner_Signoff.md` exists; treating it as agreed pending real conversation.
- **Kickoff conversation deferred.** Seeded reminders + goals are placeholders; will be replaced after the 90-min session in `01_Family_Kickoff_Guide.md`.
- **Until Twilio is provisioned**, the Reminders engine writes briefings to `/Briefings/` and drafts a Gmail to Adar as the alert channel. Switch to WhatsApp is a single function swap.

## Open questions / blockers

- Twilio account: not provisioned. Email fallback in place until then.
- Google Calendar connector: suggested, not yet connected. Briefing falls back to manual `Calendar-Events` rows until then.
- iCloud Calendar (partner): no native MCP yet. Manual entry into Calendar-Events with Source="iCloud" works as interim.
- Kids' info: ages, schools, Kupat Holim — current Sheet values are placeholders.
- Long-term goals: 2 placeholder goals seeded; replace after kickoff.

## What Cowork is NOT doing (out of scope, for now)

- Trading or moving money on Adar's behalf.
- Storing bank credentials anywhere.
- Sending messages to anyone outside Adar + partner.
- Making medical decisions; only surfacing reminders.

=== End: 00_Architecture_and_Roadmap.md ===


```

</details>
