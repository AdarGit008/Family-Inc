# Session changes — 2026-06-16, D-040 (Apify secondary source for the property lane)

Review trigger: touches privacy/cost guarantees (a paid third party enters the data path; amends §8.6 secrets + D-010 cost) and the §12.1 data model — ENGINEERING §11. Not an M-close (M5 closes after the PO deploys + verifies a live scrape).

- **New `automation/lib/apify.py`** — the only Apify client. `is_configured()`/`token()` gate on `FAMILY_INC_APIFY_TOKEN`; `_run_actor()` is the ONLY network call (POST `run-sync-get-dataset-items`, Bearer auth) and is never exercised by the suite (mirrors `fetch_html`); `fetch_listings(portal, search, runner=…)` adapts dataset items via per-portal maps. Actors (D-040 picks): `amit123~yadscraper` (Yad2 — ingests the saved-search URL directly) and `swerve~madlan-scraper` (Madlan — parametric, requires an `apify:{city,dealType,…}` block; params never guessed from the url).
- **Strict, fail-loud, no-invented-data contract** (the explicit ask): missing id or present-but-corrupt numeric → `ApifyItemError`, the item is skipped **and** the error is collected → surfaced (fail-flag); absent optional → honest empty, never fabricated; `0`/negative numeric → unknown(None)/loud, never a fake ₪0; no token when invoked / HTTP / 408 / non-JSON / non-list → loud `ApifyError`.
- **`property_scrape` wiring — SECONDARY only, primary unchanged.** `gather()` runs the on-box Chromium primary first; Apify is consulted per-search **only** when the primary is blocked/empty (backup) or returned listings with missing fields (gap-fill). `merge_listings()` unions by `listing_id` with the **primary always winning** (Apify only adds missed listings + fills blanks, never overwrites). Backup failure (both sources down) is loud; gap-fill-only failure is best-effort (warning, not a run failure).
- **Cost gate**: Apify lands at most **once/calendar-day** (`PROPERTY_APIFY_ONCE_PER_DAY`), stamped only on a durable Sheet write (aligned with the seen-set anti-poison guard); on-box primary keeps its free 2×/day; per-search item/page caps. D-010's "₪0 marginal" amended → governed by the §11 ≤₪120/mo ceiling.
- **`load_searches`** now carries a per-search `apify` block through to `build_input` (Madlan needs it; Yad2 doesn't).
- **config.py**: `APIFY_TOKEN_ENV`, `APIFY_BASE_URL`, `PROPERTY_APIFY_ACTORS`, timeout, item/page caps, `GAPFILL`, `ONCE_PER_DAY`, stamp file.
- **Tests**: `tests/test_apify.py` (29) — adapters vs documented schemas, strict missing-id/corrupt/zero/negative, token gate, input synthesis (Yad2 url, Madlan requires params), merge primary-wins, per-search once/day skip, gap-fill-doesn't-consume-backup-budget, both-sources-down loud, gap-fill best-effort, loader carries `apify`. Suite 230 → **259 green**.
- **In-session adversarial review already applied** (pre-this-review): reject `0`/negative numerics, keep the residential proxy on config overrides, de-alias the mock listings, added the both-down + gap-fill tests.
- **Firecrawl**: considered and **deferred** (PO: "no firecrawl for now") — no policy exception added.
- **Canon updates**: SPEC §12.1 (secondary-source facet, auth-model token, failure-handling escape hatch) + §8.6 secrets list; DECISIONS D-040 (Shanee in the loop); BACKLOG M5 status + remaining; CLAUDE.md current-state; ENGINEERING §2 secrets/env; `deploy/property_searches.example.json` (Madlan `apify` block + token note).
