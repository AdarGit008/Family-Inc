# Session changes — 2026-06-12, M1 repo restructure (milestone close)

- Created `automation/lib/` single implementations: `config.py` (ALL constants + paths, absorbed `Automation/config.py` and every script-level constant), `dates.py` (`to_date` incl. the DD/MM/YYYY sheet contract, `to_datetime`, `fmt_date`), `money.py` (`fmt_money`, `pct`), `sheet.py` (Reminder dataclass, tolerant `read_reminders`, §6.1 column map; openpyxl stand-in until the M2 gspread port), `llm.py` (the only anthropic wrapper; cost log; `FAMILY_INC_LLM_FAKE` test hook), `outbox.py` (full SPEC §7.5 chokepoint: per-recipient daily ledger, alert/critical/briefing kinds, (id,target) dedup, quiet-hours `not_before`, budget-deferral to tomorrow's digest, audit events; legacy `queue_message` shim kept for the summarizer until M2).
- Carved `automation/daily_digest.py` out of the engine send path (ENGINEERING §9): engine is compute-only + heartbeat log; the digest assembles ONE morning message per recipient (fires + budget-deferred + WA digest + Friday Hebcal line), writes Briefings files, queues only with `--send` (id `brief-daily-{date}`, kind=alert per SPEC §7.1). Message copy moved to `automation/templates.py`.
- Renamed `sunday_briefing.py` → `automation/weekly_briefing.py` (D-011); root-level engine + briefing deleted. Recipient identity in code is now logical lowercase `adar`/`shanee` (legacy "Partner" rows still parse).
- Bridge: `whatsapp_bridge/` → `automation/bridge/` with gitignored `state/{auth_state,inbox,outbox}`; listener gained the `not_before` quiet-hours skip; README/paths rewritten.
- Frozen lanes → `attic/` (anomaly, hebrew_categorizer, pediatric_milestones, goal_coaching, pdf_to_event, dira_tracker, bank-scraper, friday_briefing, Setup .gs code); `Progress/`, frozen-lane runbooks, and `00_Runbook.md` → `Archive/`.
- Twilio purged from all code + runbooks (acceptance #7 grep clean; fallback lives only in SPEC §10).
- `review.py`: canon-doc always-attach (CLAUDE/SPEC/BACKLOG) + lane defaults, new `milestone` lane, DeepSeek folded in as `--provider deepseek` (plain + `--chunk`), audit output → tracked `reviews/`; `run_review_deepseek.py` deleted; kids' names/ages removed from the prompt template.
- Hygiene: `Briefings/` + `logs/` gitignored (556f445 re-applied); review/audit artifacts moved to `reviews/`; hand-written future-dated briefings (2026-06-23, 2026-08-15) deleted; `tests/fixtures/` added.
- Tests 55 → **115 green**: new `test_outbox.py` (2-cap, critical bypass, briefing exemption, shared ledger across sources, dedup incl. sent-ledger, quiet hours, deferral), `test_summarizer.py` (5 hard rules, digest_only downgrade, none→NEEDS-A-LOOK, deterministic fallback, LLM-fake), `test_render_golden.py` (5 byte-exact goldens freezing as-built copy), `test_sheet.py` (parsing tolerance); renamed engine/briefing suites.
- uv conversion: `pyproject.toml` + `uv.lock`; dropped beautifulsoup4 + python-dateutil (consumers in attic); `requirements*.txt` deleted.
- **D-024 privacy purge**: seed CSVs → gitignored `seeds/` (vaccine schedules with kid names were tracked); `Dashboard/config.js` untracked (+committed `config.example.js`); kid names + birthdates scrubbed from attic mocks; home-town strings dropped from code comments/mocks.
- Docs: CLAUDE.md current-state + paths; BACKLOG M1 flipped, M2/M3 carry-overs added (recurrence-bump tests → M2 write path; Dashboard case rename → M3); ENGINEERING §1 current, §7 updated, §11 reviews/ + provider contract.
- Known flags for the reviewer: SPEC §6.4 says `Finance-Budget` but the Sheet + code use `Finance-Bdgt` (left as-is, needs a PO call); SPEC §7.1 routes the daily digest as outbox kind=alert (so it consumes 1 of the 2/day budget) while §3 exempts "scheduled briefings" — implemented as written, worth a deliberate look; dira_tracker (attic, frozen) still names target towns because that is its function.
