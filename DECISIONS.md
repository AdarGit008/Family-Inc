# Decision Log

*One row per decision: ID · date · decision · why · what it superseded. Newest first; IDs ascend chronologically (D-001 = oldest). Reference decisions anywhere as `D-0NN`.*
*This is the only place decisions live. Specs describe the current state; this file explains how we got here.*

| ID | Date | Decision | Why | Supersedes |
|---|---|---|---|---|
| D-048 | 2026-06-17 | **Phase-F weekly classifier accuracy review surface BUILT — front-loaded by PO call (D-044 precedent); the first real review run + the external milestone review stay gated to ~2026-06-20.** New `automation/accuracy_review.py` reads `WhatsApp_Inbox`, windows the trailing 7 days, and **re-derives each ALERT's triggering rule by reusing `whatsapp_summarizer.hard_rule_alert`** against the persisted row — the hard rules are deterministic functions of (group, sender_role, time, text), so this is faithful and can't drift from the classifier (single source of truth), and it needs **no Inbox schema change / live-tab migration** (§6.2 stores the classification + outcome but not which rule fired). It tallies ALERT-tier volume (critical/standard, sent/floated, by group_type, **by rule**) and states the bar — **<1 ALERT-tier false positive/week** (original WhatsApp design, Phase F). Two renders: `render_full` → `Briefings/{date}_accuracy_review.md` (the on-demand operator surface the PO runs on ~06-20); `render_brief` → a compact **"Classifier accuracy" section folded into the weekly briefing** (§7.2) so the recurring cadence **reuses `family-weekly.timer` — no new systemd unit**. The briefing section is auxiliary + **fail-safe** (any error degrades to one line, never takes the load-bearing briefing down; absent Inbox tab → "_No classifier data yet._"). Config: `ACCURACY_REVIEW_DAYS=7`, `ALERT_FP_TARGET_PER_WEEK=1`, `ACCURACY_REVIEW_MAX_BRIEF`. **Explicitly deferred (PO call):** a *machine-measured* FP rate (vs by-eye) needs a human-mark channel — an additive Inbox `review` column or a dashboard control — kept out this session to avoid an unplanned §6.2 schema/contract change; the surface as built organizes the week's ALERTs for the human judgment and points at the tunable keyword patterns (that narrowing is the "purge"). **Gate-free, read-only, additive — no budget/privacy/delivery/contract change (SPEC §11 acceptance list untouched), so no milestone-review run** (D-044/D-046 precedent); the gated external review folds it in on ~06-20. SPEC §7.2/§7.3 gain the surface. Tests **291 → 312** (+21 `tests/test_accuracy_review.py`: rule re-derivation per hard-rule type + LLM/none, trailing-window incl. undated rows, counters, both renders, `run()` over an explicit xlsx, the no-live note, the briefing section present/absent/wired); the `weekly_briefing` golden re-cut for the new section; an end-to-end smoke run rendered the surface correctly (critical-keyword / vaad-utility / LLM-context rules, sent vs needs-a-look dispositions). | Front-loading the *tooling* now (D-044 logic) means on ~06-20 the PO runs it against a full week of live DeepSeek-classified output instead of building then; re-deriving the rule from persisted fields buys the accuracy-tuning view with one source of truth and zero live migration; a real FP *rate* is a bigger contract decision (schema/affordance) better made deliberately than bolted on under the gate | the M4 ⬜ "Phase F weekly accuracy review surface" board item (now ✅ built; first run gated); the implicit absence of any classifier self-report in the weekly briefing |
| D-047 | 2026-06-17 | **Milestone-lane review of the debrief + D-046 resolved; the weekly briefing is confirmed TEMPLATE-ONLY by design (PO call, Adar) — SPEC/DESIGN corrected, LLM-written prose deferred.** Ran `review.py --provider deepseek --lane milestone` (audit: `reviews/review_milestone_2026-06-17_09-09.md`). Dispositions: **(1, HIGH — §7.2 briefing prose dormant) DECISION:** the weekly briefing has been a deterministic template since v1-live and stays that way — wiring LLM prose would send whole-Sheet context (reminders / finance / kids' health / goals) to DeepSeek, far beyond §8.6's "one message + ≤3 context" classification bound, for a cosmetic gain on a briefing nobody has faulted; "honest affordances" says match the spec to reality. SPEC §4 / §7.2 / §8.7 + DESIGN §6 reworded from "LLM-written, template fallback" to "deterministic template"; **LLM-written prose → deferred v1.1 candidate (needs a §8.6 privacy call + Shanee)**; a content review of the template itself is parked for a future session. **(2, HIGH — `_first_json_obj` fence/leading-prose) DEFEND:** the reviewer self-rescinded mid-item; impl is correct, tests cover leading/trailing/fenced. **(3, MED — D-045/D-046 lacked Shanee) SPLIT:** D-045 (who-gets-briefed, a UX call) flagged for Shanee's design confirmation; D-046 (internal parse bug-fix) is an engineering detail, Adar's solo call per the roles — DEFEND. **(4, MED — `delivery_log` trailing `\`) DEFEND (falsified vs code):** the recipients field is clean (`adar\|shanee` from the hardcoded `DIGEST_RECIPIENTS` tuple); the `\` is the CSV `\r\n` terminator surfaced by `grep`/paste — `splitlines()` strips it and the weekly parser keys on the transport field, not recipients. No change (the reviewer's `rstrip` would patch a phantom). **(5, LOW — json_mode blind spot) APPLY:** `_first_json_obj` now returns `(obj, had_prose)` and `llm_classify` logs a distinct warning when a json_mode reply still carries prose (recovered via `raw_decode`) — so a silent `response_format` regression stays visible; 291 green. **Deferred (missed-alternatives):** `_parse_dt` rebases an unparseable timestamp to `now()` (valid, pre-existing, already logs) → hardening-pass item; dead `config.MODELS["briefing"]` entry → remove at the template review. | An external review on a debrief: the one HIGH that survived scrutiny (§7.2) was a real spec-vs-code lie, and the cheapest honest fix is to correct the spec, not build a privacy-expensive feature nobody asked for; the rest were self-rescinded, mis-diagnosed (verified against code), or a genuinely useful cheap observability add — adversarial-but-fair worked as intended | the SPEC §4/§7.2/§8.7 "LLM-written briefing" + DESIGN §6 claims (now: deterministic template, LLM prose deferred); D-046's `_first_json_obj` return shape (now carries `had_prose`) |
| D-046 | 2026-06-17 | **WhatsApp classifier JSON parse made trailing-prose-tolerant + DeepSeek strict JSON mode — fixes a silent accuracy leak found live during the debrief (PO call, Adar: "fix now").** The first real DeepSeek traffic confirmed the production prompt is intact (`whatsapp_summarizer` classifications at ~180–300 input tokens, not the 11-token smoke-test ping — the empty-prompt worry is cleared), but ~1 reply in 8 hit `Extra data: line 1 column 175`: `deepseek-chat` appends commentary after the JSON object, and `llm_classify`'s plain `json.loads` (after a code-fence strip) raised ValueError → silent drop to the keyword `deterministic_classify`. Degrade-quiet, so no outage, but the cruder path ran on messages the LLM should have judged. Two-part fix: (a) `whatsapp_summarizer._first_json_obj` parses the first object via `json.JSONDecoder().raw_decode` — tolerant of leading text, trailing prose, and ```json fences```; (b) `lib/llm.complete` gained `json_mode`, which sets DeepSeek `response_format={"type":"json_object"}` (root cause; the prompt already says "JSON" as their API requires) and is passed ONLY from the classify call, so non-JSON tasks (briefing prose) are untouched. Stale "Haiku"/"only Anthropic wrapper" log+docstring labels corrected to provider-neutral. Tests **288 → 291** (+3: json_mode body present-only-when-asked, trailing-prose end-to-end through the LLM-fake, `_first_json_obj` fence/trailing/no-object matrix); the live "Extra data" shape reproduced and shown to parse. Gate-free deterministic hardening — an M4 build item pulled forward by PO call (D-044 precedent); no contract/budget/privacy/delivery change, so no milestone-review trigger (SPEC §11 list untouched). **Landed + 291 green; deployed `f46143b` and verified live on-box 2026-06-17** — an on-box `--dry-run` over 30 live inbox messages classified clean through DeepSeek with zero parse-fallback warnings (leak closed); the next hourly run is the passive confirmation. | Degrade-quiet was hiding a measurable accuracy loss behind a "successful" fallback; a tolerant parse restores LLM judgment on ~1/8 messages and also protects the Anthropic fallback (which has no JSON mode), while the strict `response_format` attacks the cause and trims wasted trailing-prose output tokens against the §11 ≤₪120/mo ceiling | D-044's classify parse (`json.loads` after fence-strip → `raw_decode` + `json_mode`); the pre-DeepSeek "Haiku" labels in `llm_classify` |
| D-045 | 2026-06-17 | **Daily briefing is partner-symmetric every day — an asymmetric day (one adult has reminders, the other none) now briefs BOTH adults (PO call, Adar; closes the D-044 open question).** **Shanee confirmed 2026-06-17** — the D-047 review fairly flagged briefing-recipient behavior as a product/UX call (not pure engineering); Shanee signed off, so D-045 is now jointly ratified. `daily_digest.assemble` filled both recipients only when the computed digests were EMPTY (the fully-quiet day, D-036e/D-044d); on an asymmetric day the dict was non-empty, so the empty-handed adult got no morning message at all — and was perversely *more* likely to be briefed on a totally dead day than on a day with family activity (their own reminders aside, the shared WhatsApp-groups / property sections never reached them). Fix is one place: `assemble` now rebuilds `digests` over `config.DIGEST_RECIPIENTS` (`{r: digests[r] if r in digests else Digest(r)}`) so both adults are always present in canonical (adar, shanee) order; the empty-handed adult renders the existing quiet-day line + the shared sections. The fully-quiet-day rule (D-036e/D-044d) becomes the both-empty special case of this general rule, not a separate branch. **Costs nothing against the alert budget** — the digest is kind=briefing (budget-exempt, never deferrable, D-027), so the second message can't consume a 2/day slot. No copy change (`DIGEST_QUIET_DAY` carries both cases) → no golden re-cut; the `digest_assembled` golden reads only adar's message and is byte-identical. Tests 286 → **288** (+2 in `TestBriefBothEveryDay`: asymmetric briefs both, shared sections reach the empty-handed adult; two `test_mailer` row-count asserts updated 1→2 for the Adar-only-fire case). SPEC §7.2 gains the recipient contract; DESIGN §6 gains the rule + a quiet-day-for-one-adult template. Gate-free (deterministic digest logic, no live data) — M4's two remaining items (Phase-F accuracy review, external milestone review) stay gated to ~2026-06-20. | "briefings > notifications" + partner-symmetry + budget-exempt briefings all point one way; after D-036e the brief-only-when-you-have-something behavior was internally inconsistent (a message on dead days, silence on active ones), and a predictable daily briefing for both is the simpler mental model — the shared sections matter to both regardless of whose reminders fire | D-044's "Open question flagged for a PO call: asymmetric-day briefing"; D-036e/D-044d's fully-quiet-only fill (now the special case); the implicit "brief a recipient only when they have fires" |
| D-044 | 2026-06-16 | **M4 gate-free build items pulled forward (DeepSeek wired · WhatsApp_Inbox rolloff · quiet-day symmetry · sender-role roster); the two review items stay gated to the ≥1-week-live window (~2026-06-20).** PO call (Adar) to spend this session on M4's *decided* build work — which doesn't need live data — rather than idle until the review gate. **(a) DeepSeek wired (executes D-032):** `lib/llm` gained an OpenAI-compatible `/chat/completions` backend over **stdlib urllib (no new dependency, no lockfile churn)**; the active provider is chosen by which key is present — `FAMILY_INC_DEEPSEEK_API_KEY` → DeepSeek (`deepseek-chat`, `config.MODELS`), else `ANTHROPIC_API_KEY` → the Haiku-class fallback (`config.ANTHROPIC_MODELS`), else the deterministic fallback (still useful keyless, §3.6). Network isolated in one `_http_post` seam (tests monkeypatch it, never the wire); `complete()` returns None on every failure (no key / HTTP / garbled / empty). **This amends SPEC §8.6/§8.7 to name DeepSeek as the single permitted external message processor** — both sections updated from "keyless until M4" to the wired state. **Keyless stays the live default until the PO places the key** — inert without it, exactly like the Apify token (D-040). **(b) Test hermeticity extended to the LLM transport:** the autouse `_hermetic_no_live_sheet` fixture now also blanks `FAMILY_INC_DEEPSEEK_API_KEY`/`ANTHROPIC_API_KEY`, so once the key lands in `/etc/family-inc/env` the appliance's `deploy.sh` pytest can't pull a real model call into a test (same class as D-038/D-041 — now closed for the Sheet, SMTP, and LLM). **(c) WhatsApp_Inbox 30-day rolloff (lands D-036d):** `sheet.roll_off_old_rows` + a `delete_rows` on both backends (contiguous-run batching on gspread) deletes Inbox rows older than `WA_INBOX_RETENTION_DAYS` after each successful append; undatable rows are kept, the seed is never touched (live-or-explicit-path only), and **WhatsApp_Archive is never rolled** (text-forever, §6.2). **(d) Quiet-day digest partner-symmetric (lands D-036e):** a fully-quiet day now briefs **both** adults (`config.DIGEST_RECIPIENTS`), not adar alone — verified live against the seed; two rerun-idempotency tests re-pointed to `Both`-owned rows to keep their "rerun is a no-op" intent. **(e) Sender→role roster:** `seeds/13_Sender_Roster_Seed.csv` (gitignored, format in `seeds/README.md`) maps a JID/push-name → role so the §7.3 hard rules 2–3 fire on real traffic the bridge can't label; `resolve_role` fills only `unknown`/missing roles (explicit roles win), so mock/tests are unchanged. Tests **262 → 286** (+24: test_llm, roster, rolloff, quiet-day, rolloff-integration). **Open question flagged for a PO call (NOT decided here):** on an *asymmetric* day — one adult has reminders, the other none — the other still gets no morning message; D-036e covered only the fully-quiet day. Candidate fix: brief both every day (briefings > notifications). **Pending PO:** place `FAMILY_INC_DEEPSEEK_API_KEY` (and optionally the roster) in `/etc/family-inc/`; M4's Phase-F accuracy review + the external milestone review remain for the ~06-20 session. | M4's *review* items need ≥1 week of live data, but its decided build items (D-036d/e, D-032) don't — doing them now front-loads the work and, crucially, makes DeepSeek summaries accrue during the live window so the eventual accuracy review has real LLM output to judge | D-032's "not wired / keyless until M4"; D-036d "rolloff code lands M4"; D-036e "quiet-day code lands M4"; the adar-only quiet-day fallback; SPEC §8.6/§8.7 keyless wording |
| D-043 | 2026-06-16 | **M5 (property tracker) CLOSED — live on the appliance.** Deployed the same day as the local build (D-037) and verified end-to-end on the VPS: the on-box headed-Chromium primary (D-039) clears **Yad2** from the datacenter IP, **Madlan** (which the on-box browser can't clear) comes through the **Apify secondary** (`swerve~madlan-scraper`, D-040), and new listings land in the `Property-Listings` tab + the silent `🏠 דירות חדשות` morning-digest section (never an alert, never budget). The deploy shook out two appliance-only bugs the credless suite couldn't see — **D-041** (tests leaked the box's `/etc/family-inc/env` SMTP creds → the email-fallback hijacked the send-stamping tests) and **D-042** (over-strict per-item Apify errors made a junk Yad2 row fatal). Both adapters verified-live against real dataset items; 262 tests green on the box; a clean `systemctl start family-property.service` wrote Yad2 + Madlan rows. Ships **no further code/contract change** beyond D-041/D-042 (doc flips + this close), so the milestone external-review gate rides the existing **M4 "review on the live system"** board item (D-035 precedent), not a separate run. **Next:** M4 (summarizer hardening) — its ≥1-week-live gate opens ~2026-06-20. | M5's SPEC §12.1 deliverable is live and the active house search now has a daily Yad2+Madlan feed; closing it records the v1-feature-complete state and points the next session at M4 | the 🔵 "built; VPS deploy pending" M5 state; `CLAUDE.md` "Next: M5 … deploy pending" |
| D-042 | 2026-06-16 | **Apify item-level error policy relaxed: skip junk rows everywhere, fail loud only when a call returns items but ZERO usable listings (broken adapter/schema).** Surfaced at the M5 deploy with real searches: the Yad2 actor (`amit123~yadscraper`) returns the occasional **id-less row** (promo/edge entries among real listings), and D-040's per-item fail-flag turned 2 such rows in a **gap-fill** into a whole-run `ScrapeError` — even though the primary data was complete and the good secondary rows parsed fine. New contract (applies to BOTH backup and gap-fill, in `property_scrape._apify_search`): a junk item is ALWAYS skipped (never invented); item errors are fatal ONLY when the call returned items yet **none** were usable (genuine schema drift / broken adapter) — when usable listings came back, the junk rows are logged + skipped, not a run failure. Also this session: both adapters' field maps **verified-live** against real dataset items and confirmed correct, no remap needed — yad2 = `item_id/item_url/price/rooms/square_meter/location`, madlan (`swerve~madlan-scraper`) = `id/url/price/rooms/areaSqm/address/neighbourhood/cityHebrew`; Madlan's Apify backup already succeeded in the failing run (the only error was the Yad2 gap-fill junk). PO call (Adar, "relax both paths for now"). 262 tests (+3: backup-junk-best-effort, gapfill-junk-best-effort, all-items-bad-fails-loud). | Real portals return dirty rows; failing an otherwise-complete run on a couple of id-less promo entries is brittle and spams the fail-flag. "Loud only when zero usable" keeps D-040's no-silent-failure for true breakage while tolerating dirty data — symmetric with how a gap-fill *transport* error is already best-effort | D-040's per-item "skipped AND surfaced → fail-flag" clause (now: skipped always; surfaced→fail-flag only when the whole call yielded zero usable listings) |
| D-041 | 2026-06-16 | **Test suite made hermetic vs the appliance's email-fallback creds (extends D-038 from the Sheet to the SMTP transport).** Caught at the M5 deploy: `deploy.sh`'s pytest went red on the appliance only — `TestSendStamping::{test_send_stamps_then_rerun_is_noop, test_send_without_queue_success_does_not_stamp}` — while green on every dev box. Root cause = the same env-leak class as D-038: `config.load_env()` (setdefault, "existing env wins") injects `/etc/family-inc/env` into the test process via `sheet.is_live()` **and** `mailer.send_digest()`; under `tmp_runtime` the heartbeat file is absent → the digest takes the §10.2 email-fallback branch, and with the box's real `SMTP_USER/PASS` present it **actually sends**, so the WhatsApp-outbox assertions (outbox file written; stamp-only-on-real-send) broke. Fix: the autouse `_hermetic_no_live_sheet` fixture now also sets `SMTP_USER`/`SMTP_PASS`/`FAMILY_INC_EMAIL_TO` EMPTY, so `send_digest` short-circuits (no creds) and tests fall to the queue path on any box; `test_mailer` still sets its own creds (runs after the autouse, so it wins). Verified by reproducing the box failure in the sandbox (ambient creds + a fake-success SMTP: 2 failed → after fix 3 passed) then the full suite green both clean (259) and under the simulated appliance env (259). No production behavior change — the email fallback itself is untouched; only the test harness was leaking. | The M5 deploy gate (`deploy.sh` aborts on red tests) was blocked by tests that were never hermetic against the box's SMTP creds; D-038 fixed exactly this for the Sheet but only for `FAMILY_INC_SHEET_ID`, leaving the email transport exposed | D-038's Sheet-only env neutralization (same autouse fixture, now covers the SMTP/email-fallback vars too) |
| D-040 | 2026-06-16 | **Apify added as the property lane's SECONDARY data source (managed-unblocker rung; the on-box scraper stays primary).** D-039's verdict stood — headed-Chromium-under-Xvfb still drew Cloudflare/PerimeterX challenges, so the blocker is the VPS **datacenter IP**, not the browser. Rather than the deferred rungs (`channel="chrome"`, email-pivot), the PO chose a managed scraper that runs from its own **residential proxy pool**. Shape: new `automation/lib/apify.py` is the only Apify client (network isolated in `_run_actor`, never hit by the suite — same pattern as `fetch_html`); `property_scrape` consults it **per saved-search only when the primary is blocked/empty (backup) or returned listings with missing fields (gap-fill)**, then `merge_listings` unions by `listing_id` with the **primary always winning** (Apify only adds missed listings + fills blank fields, never overwrites). Actors (D-040 picks, in `config.PROPERTY_APIFY_ACTORS`): **`amit123~yadscraper`** for Yad2 — ingests the saved-search **URLs we already have**; **`swerve~madlan-scraper`** for Madlan — parametric, so a Madlan search needs a `{city,dealType,…}` `apify` block in `property_searches.json` (refuses to guess params from the url → loud). **Strict contract (the ask): no silent failure / no invented data** — missing id or present-but-corrupt numeric → item skipped **and** surfaced (collected error → fail-flag); absent optional → honest empty, never fabricated; no token when invoked / HTTP / 408 → loud `ApifyError`. **Cost:** Apify is priced per result, so D-010's "₪0 marginal" gives way to the **§11 ≤₪120/mo ceiling** (still governs) — bounded by a **once/calendar-day** gate (on-box primary keeps its free 2×/day; digest is morning-only) + per-search item/page caps; gap-fill failure is best-effort (warning, not a run failure), backup failure is loud. Secret `FAMILY_INC_APIFY_TOKEN` → `/etc/family-inc/env` mode 600 (§8.6; a SERVICE key, not a portal login — "no credential storage" intact); **absent token = the whole path is inert** (primary-only), so the appliance is untouched until it's placed. **Firecrawl considered and DEFERRED** this session (PO: "no firecrawl for now") — no policy exception added. **Shanee in the loop** (joint call). **Milestone review run 2026-06-16** (`reviews/review_milestone_*.md`): 1 CRITICAL applied — the once/day gate made **per-search + per-kind** so a cosmetic gap-fill can't spend the load-bearing backup's daily budget and silently suppress a later blocked-primary backup (and one search can't suppress another); NITs defended (0→honest-None not a fake value, in-place merge is locally-owned). Residual the review named: the §11 ≤₪120/mo ceiling is **monitored, not programmatically enforced** — bounded by per-search/per-day calls + item/page caps, with a monthly result-counter cap left as a v1.1 follow-up. 259 tests green (+29, `tests/test_apify.py`); untestable live in the sandbox (no token/network) — verified suite-green + import-clean + mock-run, proven on the box at deploy. **Pending PO deploy:** add the token, add Madlan `apify` blocks, run once + verify a live scrape lands `Property-Listings` rows — **M5 closes then.** | D-039 proved the IP is the wall; a managed proxy pool clears what the on-box browser can't while keeping the on-box primary, the digest-only/silent delivery, and one-source-of-truth all intact — and the strict adapter satisfies the "no silent failure / no invented data" requirement directly | D-039's "next rung deferred" (picks the managed-unblocker rung); D-010's absolute ₪0 for this lane (now the §11 ceiling); §12.1 "scrape only / no third party in the path" (now: primary scrape + a permitted secondary fetch provider) |
| D-039 | 2026-06-16 | **Property scraper → headed Chromium under Xvfb + light stealth, to pass the Yad2/Madlan anti-bot wall (the D-034 escape hatch, first/free rung).** M5's first live scrapes with **real** saved-search URLs were served Cloudflare (Yad2) / PerimeterX (Madlan) **challenge pages** — vanilla headless Chromium from the VPS's datacenter IP is detected. PO chose the free on-box fix over a paid proxy or an email-alert pivot. Change: `fetch_html` launches `headless=False` with `--disable-blink-features=AutomationControlled` (+ `--no-sandbox`, `--disable-dev-shm-usage`) and a `navigator.webdriver` mask, then polls until the JS interstitial clears or the per-URL timeout; `family-property.service` ExecStart is wrapped in `xvfb-run -a`, and `provision.sh` §4b now also installs `xvfb`+`xauth`. Browser still stays out of the lockfile (`uv run --with playwright`, D-018). **Honest:** headed+stealth beats the headless tell, but a datacenter IP may still be challenged; if so the next rungs are `channel="chrome"` (real Chrome binary), a residential-proxy/unblocker (paid, breaks D-010), or the email-alert pivot — all deferred. Lane still fails loud (BlockedError → fail-flag) when blocked; `--no-sandbox` accepted for an unprivileged, `NoNewPrivileges` scraper of untrusted pages. Untestable in the sandbox (no live network/browser, different IP) — verified only suite-green + import-clean; proven on the box. | Real URLs proved it's the anti-bot wall, not config; the PO picked the free/boring rung (D-010) before paying or re-architecting | the headless-only `fetch_html`; the plain `ExecStart` |
| D-038 | 2026-06-16 | **Test suite made hermetic vs the live Google Sheet; `deploy.sh` strips `FAMILY_INC_SHEET_ID` for tests (caught at M5 deploy).** M5's first `deploy.sh` on the appliance failed one test: `test_no_backend_does_not_advance_seen` runs `run(dry_run=False, sheet_path=None)` expecting no backend, but `sheet.is_live()` → `config.load_env()` reads `/etc/family-inc/env` where `FAMILY_INC_SHEET_ID` is set — so the test took the **live** backend, advanced the seen-set, and (service-account present on the box) **appended the 3 mock listings to the live `Property-Listings` tab** (auto-creating it). It passed in CI/sandbox only because there is no env file there. Two-layer fix: **(1)** a conftest **autouse** fixture sets `FAMILY_INC_SHEET_ID=""` for every test — `load_env` is "existing env wins", so empty beats the file and `is_live()` is False on any box (`delenv`/`-u` would let the file repopulate it); **(2)** `deploy.sh` runs `FAMILY_INC_SHEET_ID= uv run pytest -q` (defense-in-depth); **(3)** `property_scrape.run()` now **fails loud** if MOCK sample data meets a live Sheet with no explicit path (i.e. `property_searches.json` missing on the appliance, e.g. the timer firing before it's placed) — raises `ScrapeError` → fail-flag, writing nothing, instead of appending `yad2:mock-1`-class rows. Invariant tightened: M2's "creds-less runs never write the seed" now extends to "**the test suite never reaches the live Sheet**". `set -e` had already halted the deploy before the bridge restart — the gate worked; no engine/contract logic changed beyond test isolation + the deploy guard. Cleanup: the mock rows (`yad2:mock-1/2`, `madlan:mock-3`) removed from the live tab by hand. | A deploy-time `pytest` that can reach the live source of truth is a latent data-corruption path; tests must be environment-independent | the env-dependent `is_live()` exposure in the test suite; `deploy.sh`'s unguarded `uv run pytest -q` |
| D-037 | 2026-06-15 | **M5 property-tracker — local build landed (executed by PO; built one day before the §12.1 "earliest 2026-06-16" gate).** v1 was accepted + M3 closed earlier today (D-035), so the gate's sole rationale — "no new code on the appliance mid-acceptance" (D-029 stability) — is already satisfied; this session writes **repo-local code + tests only**, no appliance touch (Chromium provisioning + the first live scrape happen at the PO's deploy step). Built per SPEC §12.1: `automation/property_scrape.py` (saved searches → lazy-Playwright headless Chromium → embedded-JSON card extraction + tolerant normalize → `seen.json` diff → `lib/sheet` append to a new `Property-Listings` tab → silent "🏠 דירות חדשות" section folded into the 07:30 digest); `family-property.{service,timer}` (07:10/19:10, `TimeoutStartSec`/`MemoryMax`, `OnFailure` fail-flag); `provision.sh` §4b. Notable in-session calls: **(a)** Playwright stays **out of the core uv lockfile** — the unit runs `uv run --with playwright` and `provision.sh` installs the browser once (keeps the core boring / one-runtime, D-018; no `uv.lock` churn); **(b)** `listing_id` is **namespaced `{portal}:{id}`** so Yad2 and Madlan id-spaces can't collide in the seen-set or the tab; **(c)** an anti-bot page raises `BlockedError` **after** persisting the successes → systemd `OnFailure` → next digest reports (fail loud, §10.2), while a genuine empty-results page returns `[]` (quiet); **(d)** delivery is digest-only/silent — no `lib/outbox` path added (no chokepoint bypass, §8.1). 229 tests green (+25 in `tests/test_property.py`), after an **in-session adversarial review** (subagent, not the external `review.py` ritual) surfaced and got 3 robustness fixes applied pre-commit: a **numeric-price** listing heuristic (promo/SEO objects in the embedded JSON no longer become phantom rows), **per-URL fault isolation** in `gather` (one flaky portal can't drop the others' listings), and **gating the seen-set advance on a successful Sheet write** (a backend-less run — smoke test, or the timer firing before `FAMILY_INC_SHEET_ID` lands — can no longer poison `seen.json` and silently lose a day's listings). **No external `review.py` pass + no M5 close this session** — M5 closes after the PO deploys and a live scrape is verified; the formal external review folds into the M4 "review on the live system" item (D-035 precedent). Property-section copy is `[Shanee review]`; DESIGN §6 gains the section at that pass. | Executing the D-034-approved lane; an active house search shouldn't idle a day when the only gate-reason (mid-acceptance appliance stability) is moot post-acceptance, and a code-only build keeps the appliance untouched until the PO deploys | the M5 ⬜ board items (now ✅/🔵); the implicit "wait until 2026-06-16" where it only guarded the appliance, not the repo |
| D-036 | 2026-06-15 | **Hardening pass + open-question close-out (special session; plan-only, executed by PO; Adar + Shanee sign-off).** (a) Three **Cowork scheduled tasks deleted** (`family-inc-daily-reminders` 07:30, `family-inc-sunday-briefing` Sun 18:00, obsolete `family-inc-m3-acceptance-closeout`): the two recurring ones predate D-018 (one VPS) + the remake, ran `daily_digest`/`weekly_briefing` creds-less in the Cowork sandbox against the seed xlsx — misleading local `Briefings/` + `reminders_log` rows, seed-mtime touches; the live `family-digest.timer`/`family-weekly.timer` own these. (b) D-033 orphans physically deleted (`attic/hebrew_categorizer.py`, `attic/anomaly_detector.py`; 0 live refs); `attic/dira_*` kept (M5, D-034). (c) **Appreciation ticker REMOVED** from the dashboard (index.html section, `renderAppreciationTicker()` + `relativeTime()` helper + i18n `ticker.*` keys, sw.js shell-cache v4→v5) and struck from DESIGN §6 — supersedes the ticker in D-004 and the DESIGN §6 layout/states/interaction/smoke references. (d) **WhatsApp_Inbox rolloff = 30 days** (resolves the D-025 90-vs-30 disagreement): SPEC §6.2 aligned down to the existing `config.WA_INBOX_RETENTION_DAYS = 30`; rolloff code still lands M4. (e) **M4 joint calls ratified:** family-group criticals do **not** override digest-only routing (critical_keywords already bypass, §7.3); quiet-day digest is made **partner-symmetric** (code M4); DeepSeek provider (D-032) **confirmed by Shanee** — wiring proceeds at M4. (f) Doc fix: ENGINEERING §3 named a committed `config.toml` that does not exist — all non-secret constants live in `automation/lib/config.py`; §3 corrected. (g) **No external watchdog** kept as accepted residual — a hard VPS-down is silent (SPEC §9 "external check optional, v1.1"); reinstatement = healthchecks.io free dead-man ping, deferred. No engine/contract logic changed beyond the doc/number/UI edits above. | Post-acceptance hygiene + closing every open question in one pass: a shadow runtime vs the seed violates one-source-of-truth/one-runtime; the rest were decided-but-unrecorded or cheap doc drift | the 3 Cowork tasks; D-033's deferred delete; D-004 + DESIGN §6 ticker; D-025 open disagreement; the M4 open joint calls; ENGINEERING §3 config.toml claim |
| D-035 | 2026-06-15 | **M3 (go-live) CLOSED — v1 accepted, tagged `v1-live`.** The SPEC §11 acceptance window (2026-06-13→15, clock from the D-029 Baileys-7 re-pair) passed: the morning WhatsApp digest reached both phones three consecutive days and a Done→recurrence bump was observed in the reminders log. `CLAUDE.md` "Current state" flipped from "not live yet" to **live**; `BACKLOG.md` M3 acceptance ✅; `v1-live` annotated tag on the all-green tree. **Build order from here: M5** (property tracker — build now, D-034) **then M4** (summarizer hardening — waits ≥1 week live). This close ships **no code or contract change** (doc flips + tag only), so the milestone external-review gate (protocol step 5) rides its existing **M4 "review on the live system"** board item rather than a redundant run now. Also folded into this commit: a stray local write to the `Family_OS.xlsx` seed (two tentative Hebcal observance rows) reverted — not a deliverable, and a reminder that the M2 "creds-less runs never write the seed" invariant deserves a glance; and the spent `DATA_FETCHING_SESSION_PROMPT.md` planning prompt removed (its session landed in D-031–034) | Acceptance is the go-live gate (SPEC §11); the tag marks the first all-green v1 tree and the v1→v1.1 boundary | the 🔵 acceptance-in-progress state; CLAUDE.md "not live yet — M3 next" |
| D-034 | 2026-06-13 | **Property tracker (Yad2/Madlan) UNFROZEN — active house search (data-fetching planning session).** The only lane unfrozen this session. Build scheduled **after the v1 3-day acceptance window closes (earliest 2026-06-16)** — not during it (no new code on the appliance mid-acceptance, D-029 stability lesson); independent of finance. Design (full spec → SPEC §12.1): saved-search URLs per portal in `/etc/family-inc/property_searches.json` (criteria are personal, never in repo, D-024); headless Chromium on the VPS (reuses the finance lane's browser dep — one dep, not two) on a `systemd` timer 1–2×/day; extract listing cards, diff `listing_id` against `/var/lib/family-inc/property/seen.json`, new → a new `Property-Listings` Sheet tab via `lib/sheet`. **Delivery = silent landing + a "New listings" section in the 07:30 digest** — never an alert, never a budget-bypass (briefings > notifications, D-008 spirit; property is not critical-safety). Scrape break / anti-bot block (Yad2 runs Cloudflare; 2026 fragility rising) → fail-flag → next digest reports it, weekly surfaces persistent failures; escape hatch is an anti-detect browser, still one runtime | DoD #1+#2: per-item PO request satisfied (active search), and an active search shouldn't wait the full 30-day gate while still respecting the 3-day acceptance window; digest-only keeps it inside the alert-budget principle | "property trackers" in SPEC §4 frozen list + the L6 frozen bundle |
| D-033 | 2026-06-13 | **Frozen-lane dispositions (data-fetching planning session).** (a) **Finance ingestion (L1) stays frozen** — no commitment yet to the ~20–30 min/month review the data needs to stay honest; condition unchanged. (b) **Hebrew categorizer (L2) + anomaly/subscription detector (L3) KILLED** — removed from the board; `attic/hebrew_categorizer.py` + `attic/anomaly_detector.py` now orphaned (physical delete left to a future code session — this one is planning-only). On record: if finance ever thaws it delivers **raw, uncategorized** transactions; categorization + anomaly would be net-new lanes, not a resurrection. (c) **Pediatric (L4) stays frozen as the full engine**, condition ("Health tab actively maintained") unchanged — PO declined the convert-to-Reminders-seeder simplification. (d) **Goals (L5): the `Goals` tab already exists in the model (§6.4); coaching automation (`goal_coaching.py`) stays frozen** under the unchanged "weekly for a month" condition — the tab gives the §8.1/§11 briefing goal-section a home while the habit forms, no code. (e) **L6 unchanged except Dira (D-034)**: Gmail bill parser, Maccabi forwarders, PDF→event, receipt OCR, voice capture, and iCloud→GCal all stay frozen / per-item-on-request (PO did not reclassify iCloud→GCal) | DoD #1: every frozen lane gets an explicit PO call; killing L2/L3 trims weight the PO judged not worth resurrecting; deferring (not killing) L1/L4 keeps optionality cheap (attic costs nothing) | L2/L3 frozen-lane rows; the "model vaccines as Reminders rows" simplification proposal |
| D-032 | 2026-06-13 | **LLM provider direction = DeepSeek (joint M4 call made early; Adar in-session, Shanee to confirm).** The open M4 provider call (Anthropic §8.7 vs DeepSeek) resolved toward **DeepSeek everywhere** — categorizer (if finance thaws), weekly-briefing prose, group-message classification — via an OpenAI-compatible backend in `lib/llm` (~30 lines + tests). Accepted tradeoff, made explicit: routes group plaintext + whole-Sheet briefing context through PRC-jurisdiction infra, which **amends SPEC §8.6's "no third-party message processors"** to permit the single configured provider. NOT wired in this planning session: v1 stays **keyless** (keyword classification + template briefing) until **M4** wires it and Shanee confirms; §8.6/§8.7 carry a forward-pointer to this row but keep describing the current keyless state until then (specs describe current state, decisions explain direction) | Categorizer (frozen) and M4 both gate on the provider; setting direction unblocks both. Cost is negligible at household volume either way, so the call is privacy-vs-jurisdiction — which the POs own | §8.7 "Haiku-class" default and §8.6 absolute no-third-party-processor wording (prospectively, at M4 wiring) |
| D-031 | 2026-06-13 | **Finance-ingestion build architecture pre-resolved while the lane stays frozen (data-fetching planning session).** Runtime = **the VPS, not Render**: the bank scraper (`israeli-bank-scrapers`, v6.7.x, puppeteer/headless-Linux) runs as a `systemd` timer (06:00 Asia/Jerusalem), not a Render cron — `systemd` `TimeoutStartSec`/`MemoryMax` give the browser-isolation that was Render's only real advantage, so finance does **not** justify a D-018 second-runtime exception (₪0 marginal vs ~₪25/mo, one secret store `/etc/family-inc/bank_creds.json`, one failure domain). Staging = **none**: scrape → local CSV on the VPS (`/var/lib/family-inc/finance/`) → Python ingest → `lib/sheet` (sole Sheet writer, D-016); the `Finance_CSVs` Drive folder is dropped (Drive-as-queue is an extra auth + sync failure mode against one-source-of-truth). 2FA (Max/Cal): first run interactive (PO enters OTP once, session persisted), later re-challenges fail loud (§10.2 fail-flag → digest), never silent. Lane stays **frozen** (no monthly-review commitment, D-033) — this only fixes the design so a future thaw doesn't re-litigate it | Render plan in `attic/bank-scraper/render.yaml` predated D-018; tested against the one-VPS principle it holds; recording the build shape now prevents re-debate at unfreeze | `attic/bank-scraper/` Render-cron + Drive-staging plan |
| D-030 | 2026-06-12 | **Publication rewrite kit + privacy posture (M3 session 3).** The D-027f-deferred history rewrite scoped *empirically*, not from memory: a token scan over every blob of every ref found personal data in 24 dead paths (path-strip: the 8 Archive personal docs at current AND pre-M1 locations, `Briefings/`, `Progress/`, `Dashboard/config.js`, the four Setup seed CSVs, `Setup/code/`, `Automation/vaccines_due.csv`) plus old blobs of ~15 kept files (replace-text: kid names incl. one Hebrew form, pediatrician, both kid DOBs as literal dates, Adar's email, kickoff health/money fragments). Two PO calls (Adar, routine): **(a)** the house-build town is personal — stripped from all history *including 12 HEAD residuals* in `attic/` ×4, `reviews/` ×7, `Archive/02_Yad2` that survived D-024's "home-town strings dropped"; **(b)** `seeds/README.md` health-backlog phrasing genericized (family size + an infant condition were inferable; D-030b). Mechanism: `deploy/publish.sh` = fresh mirror clone → `git filter-repo --invert-paths --paths-from-file deploy/publish_paths.txt --replace-text seeds/redact.txt` → gauntlet (every stripped path + every redaction LHS grepped across `rev-list --all`; any hit aborts before push) → confirmed force-push (heads+tags). Redaction strings live ONLY in gitignored `seeds/redact.txt` (PO machines + backups) — the committed kit names categories, never values. Rehearsed on a sandbox clone: gauntlet clean, independent case-insensitive Latin+Hebrew sweep clean, 24/24 commits kept, 204 tests green on the rewritten tree. Operational sequence (runbook §6): push kit → publish.sh → re-point VPS clone + fresh-clone Macs (carry gitignored personals) → flip public → VPS remote to credless public URL → revoke provision PAT → Pages (§7). Pre-rewrite GitHub-side unreachable objects: accepted (repo never public; delete+recreate documented as the paranoia option) | Pages needs a public repo, which makes D-027f's deferral due now; scan-derived lists beat recollection — the scan caught residuals D-024's manual pass missed | D-027f rewrite deferral; town + health-phrase HEAD residuals as implicitly accepted |
| D-029 | 2026-06-12 | **Bridge → Baileys 7.0.0-rc13 + ESM (go-live night fix).** Day-1 symptom: every bridge→Adar message stuck as "waiting for this message" on Adar's phone while Shanee's leg delivered — the bridge pairs as a companion on Adar's account, so his leg is a SELF-SEND (companion encrypting to own primary's device list), exactly what the LID identity migration broke in the pre-LID 6.7.x line (upstream's own 6.7.21 notes: "move to 7.0.0-rc ASAP"). Fix: pin `7.0.0-rc13` (exact — no caret drift mid-acceptance), listener converted to ESM (v7 dropped CJS; `import.meta.dirname`, engines ≥20.11), Long-tolerant `messageTimestamp` reads, reply-guard LID caveat documented (replies stay parked, v1.1). Auth boundary: v7 auth_state adds lid-mapping/device-list/tctoken key types → wipe `state/auth_state/` + fresh re-pair at upgrade; **the §11 acceptance 3-day clock starts only after re-pair, first countable digest = first morning after**. recipients.json stays PN-form; v7 resolves PN→LID on send. Verified against a sandbox rc13 install (all 4 imported APIs + socket options survive); 204 tests green. Alternatives rejected: 6.7.x @lid workaround (same bug class upstream, #1964), digest-by-email for Adar only (breaks partner-symmetric + §10.2 is a fallback, not a transport choice), bridge on its own number (against D-010 free/boring) | Bridge is unusable for half its recipients until decrypt works; rc is the upstream-recommended line (6.7.x is EOL-legacy against 2026 WhatsApp) and the rc risk is bounded by exact-pin + the §10.2 email fallback landed in D-027 | Baileys ^6.7.0 (CJS); restore-auth_state-before-re-pair as the universal rule (v7 boundary is the exception) |
| D-028 | 2026-06-12 | **M3 session-1 review run (DeepSeek, post-push — the in-chain gate ran MOCK twice: Ollama cloud 403'd the 671b model mid-day, then a placeholder key; §11 failed-review-never-blocks honored, real review same hour) and resolved: 2 applies, 3 defends, 1 PO call.** Applies: (1) fail-flag clear made line-precise — removes exactly the lines reported, so a unit failing *while* the digest runs survives to the next one (reviewer's mechanism was wrong — the next digest does report a prior digest failure — but it exposed this adjacent race); (2) transport observability — every `--send` run logs baileys/smtp/queued-stale to `logs/delivery_log.csv`. Defends (all checked against code, not the summary the reviewer saw): backup prune already env-driven (`BACKUP_KEEP_DAYS`); the Node listener *was* moved to `/etc/family-inc/recipients.json`; zero tests read gitignored seeds (suite uses the tracked seed xlsx + fixtures — fresh-VPS deploy.sh is safe). PO call (Shanee+Adar posture, Adar in-session): **email-fallback days are degraded, not green** — weekly briefing surfaces "N of last 7 digests by email"; SPEC §11 acceptance #1 now counts WhatsApp delivery only. Tests 191 → 199. Audits: `reviews/review_milestone_2026-06-12_13-{27,30,35,37}.md` | A dying bridge must not hide behind a working fallback; and review findings get checked against the diff before being applied — two of five concerns were artifacts of reviewing the summary | Unconditional fail-flag unlink; invisible fallback transport; "any delivered digest is green" |
| D-027 | 2026-06-12 | **M3 session-1 contract resolutions** (delivery infrastructure). (a) Daily digest queues **kind=briefing** — it was kind=alert, consuming 1 of the 2/day alert slots and, worse, deferrable by the ledger *into the next digest* (circular, since over-budget alerts defer INTO the digest, D-025/D-026); SPEC §7.2 clarified. (b) SPEC §10.2 email fallback **built** (`lib/mailer.py`, the only smtplib import): heartbeat stale >24h → digest goes by SMTP, identical content, stamps normally; both-transports-down queues to the bridge outbox and shouts. Documented as transport substitution, NOT an outbox bypass — content already passed outbox policy. (c) `recipients.json` moved to `/etc/family-inc/` (code now matches ENGINEERING §2; local file = dev fallback). (d) Pages serves `dashboard/` via Actions workflow (branch-mode can't serve subdirs); gitignored `config.js` is **generated from Actions secrets** at deploy — ids reach the live site without entering git history (D-024 lineage). (e) Fail-flag semantics: `OnFailure=family-fail-flag@%n.service` appends to `logs/fail.flag`; the next **delivered** digest reports + clears; weekly briefing surfaces still-uncleared flags. (f) D-024 completion: kid names/health details scrubbed from 10 tracked `Setup/`+`reviews/` files the purge missed, and 8 personal `Archive/` docs (kickoff output, money baseline) untracked + gitignored — Archive stays the public paper trail minus personal data; git *history* still holds old blobs, rewrite deliberately deferred | Go-live runs on this plumbing; each item was a SPEC-vs-code disagreement or an unwired contract surfaced while writing `deploy/` — PO (Adar) called all six in-session | Digest kind=alert; bridge-dir recipients.json; unspecified Pages mechanism; unwired OnFailure hook; tracked personal Archive docs |
| D-026 | 2026-06-12 | **M2 milestone review run (DeepSeek) and resolved: 2 wording applies (SPEC §8.3 one-clock tombstone semantics; §7.1 validate-before-write clause), 5 defends, 0 open — no direction changes.** Notable defends: deferral is consumption into tomorrow's digest (no re-queue loop); no aging-into-critical bypass (erodes the 2/day principle); schema guard stays uniform across backends. Full table appended to the audit: `reviews/review_milestone_2026-06-12_10-45.md` | Milestone-only ritual: M-close qualifies (D-012); blocking gate inside the handoff chain (D-023) | — |
| D-025 | 2026-06-12 | **M2 contract resolutions** (one source of truth port). (a) SPEC §7.1 errata: engine reads Status ∉ {Done, Skipped} — the spec'd ∈ {Pending, Snoozed, Overdue} would kill multi-lead-time chains after the first `Sent` stamp; same-day re-fires blocked by a Last-Sent guard instead. (b) Machine datetime stamps (Last Sent/DoneAt/Tombstone) = ISO-8601 `T`-form TEXT on both surfaces; dashboard tombstones re-stamped at flush time and carry hour resolution (date-only tombstones had silently disabled the §8.3 race guard). (c) Col H is engine-owned — dashboard stopped writing it on ✓done. (d) Recurrence bump = one rule both sides (`lib/dates.bump_due` ↔ `bumpDate()`): month-end clamp + review flag; Custom never guessed, flagged. (e) Live hot-tab rolloff deferred to M4. (f) Reply-ack outbox kind (solicited vs the unsolicited budget) = open PO call at v1.1 reinstatement | The gspread port made both surfaces write the same sheet for real — every place they disagreed (or quietly broke a guard) had to be resolved before go-live | SPEC §7.1 status-set wording; dashboard H-writes; date-only tombstones; JS date-overflow bump |
| D-024 | 2026-06-12 | **Personal-data purge of the tracked tree (M1).** Seed CSVs (reminders/vaccines/goals/group-config) → gitignored `seeds/`; `Dashboard/config.js` (real emails, Sheet ID slot) untracked with `config.example.js` committed; kids' names+birthdates scrubbed from attic mocks and the review-prompt template; home-town strings dropped from code comments/mocks. Committed code now carries placeholders only | D-013 says portfolio-grade public repo; the audit found names, a child vaccine schedule, and a real email tracked — enforcement, made explicit so it survives future sessions | Tracked seed CSVs; tracked `Dashboard/config.js` |
| D-023 | 2026-06-12 | **Port Porto workflow patterns:** D-numbered decisions, `Automation/session_kickoff.py` regenerating `NEXT_SESSION_PROMPT.md` each session end, and the single terminal handoff block ritual. NOT ported: separate `state.md` (status stays only in `BACKLOG.md`), per-commit blocking gate (milestone-only reviews stand; the gate is blocking inside milestone-close handoff chains) | Porto's continuity system solves the exact failure mode of the Hermes divergence; Family Inc keeps its one-source-of-truth and lighter review cadence | Ad-hoc session handoffs |
| D-022 | 2026-06-12 | **Hermes parallel sprint integrated.** A second AI session ("Hermes") had pushed 15 commits to origin from another clone; 10 code commits cherry-picked onto the remake (tests ×55, `config.py`, reply parsing, quiet-hours/batch-window impl, fixes), 5 doc/status commits superseded by the remake. "Hermes" naming **not adopted**. Originals preserved on `remote-backup` branch | Real work must not be lost; remade docs supersede its doc edits | Hermes doc commits A1–A4, Progress-page status |
| D-021 | 2026-06-12 | **Sessions must `git pull --ff-only` before any work; origin is the sync point between agents** (session protocol step 0) | Two sessions diverged silently for a week — local audit was stale against pushed work | Implicit "local folder is current" assumption |
| D-020 | 2026-06-12 | Remake milestone review run (DeepSeek; Gemini unavailable this session) and resolved: 6 applies (schema-drift guard, data-driven tombstone tuning, specified email fallback, review.py contract, auth-state restore note, no-digest runbook), 5 defends, 0 open — no direction changes. Audit: `Briefings/review_remake_2026-06-12.md` | Milestone-only ritual: a new spec qualifies | — |
| D-019 | 2026-06-11 | **Full remake.** Canon docs = `SPEC.md` + `ENGINEERING.md` + `DESIGN.md` + `DECISIONS.md` + `BACKLOG.md`; superseded docs → `Archive/` | 4 competing sources of truth for status; doc sprawl outpaced shipping | All numbered docs |
| D-018 | 2026-06-11 | Runtime = **one VPS** ("the appliance"): Baileys bridge + systemd timers + all Python | Bridge needs always-on anyway; no home hardware; one failure domain | Cowork scheduled tasks, Render cron, "always-on home machine" |
| D-017 | 2026-06-11 | v1 scope = **keystone + group summarizer**; six lanes frozen (see `BACKLOG.md`) | Ship the loop that justifies the system; breadth-first is how nothing went live | "All lanes active" |
| D-016 | 2026-06-11 | Python Sheets access = **gspread + service account**; local `Family_OS.xlsx` demoted to seed template | openpyxl-vs-gapi split = two diverging sources of truth, violating principle #1 | openpyxl reads of local xlsx |
| D-015 | 2026-06-11 | **Alert budget enforced at the outbox** (single daily ledger, all senders), not per-script | Engine + summarizer each kept their own 2/day counter → combined 4+/day possible | Per-script budget counters |
| D-014 | 2026-06-11 | WhatsApp messages **must not advertise reply commands** until reply parsing ships (v1.1) | Current templates promise `✅ done` replies that go nowhere — dishonest UI | Reply footers in message templates |
| D-013 | 2026-06-11 | Docs are **portfolio-grade**: English, self-contained, public-repo-ready; personal data lives only in gitignored config/seeds | Doubles as a showcase artifact for Adar's job search (Goal 2) | Internal-only doc tone |
| D-012 | 2026-06-11 | Review ritual = **milestone-only** (new spec, architecture shift, go-live), via `review.py`, best available external model, Gemini default | Per-session reviews drifted (DeepSeek/Ollama substitutions); codify reality | Per-session mandatory Gemini review |
| D-011 | 2026-06-11 | Currency = **ILS everywhere**; weekly briefing = **Sat 21:00** | Kickoff mixed USD/ILS; docs disagreed Sat 21:00 vs Sun 18:00 | Mixed currencies; Sun 18:00 |
| D-010 | 2026-06-04 | WhatsApp delivery = **self-hosted Baileys outbox**; Twilio dropped to documented fallback | ₪0 marginal, no WABA verification, no template approval, free-form Hebrew | "WhatsApp via Twilio" (D-001) |
| D-009 | 2026-06-02 | Bridge = **self-hosted Baileys** (not Whapi/Green API); all 5 groups in, incl. family | Free + plaintext never leaves the box we control | Whapi.cloud fast path |
| D-008 | 2026-06-02 | **Per-group alert routing** (daycare/building → both; student → Adar; family/neighborhood → digest-only) + **critical-keyword bypass** of the daily cap | Relevance is per-group; emergencies must never queue behind mundane alerts | Global routing; flat 2/day cap |
| D-007 | 2026-05-30 | Offline model = **optimistic queue + per-row tombstone** (`WriteQueue_Tombstone`, 6h skip window) | No cognitive load offline, no engine race | Explicit offline lock (disabled buttons) |
| D-006 | 2026-05-30 | **No PWA push.** WhatsApp is the only alert channel | One channel, one budget | PWA web-push proposal |
| D-005 | 2026-05-30 | Dashboard chrome **Hebrew default, RTL**, English fallback toggle; data values stay Hebrew | Household reads Hebrew; toggle costs little | English-default chrome |
| D-004 | 2026-05-30 | Appreciation ticker grouped by **domain**, names inline | Avoid leaderboard/scoring read between partners | Person-grouped ticker |
| D-003 | 2026-05-30 | Palette = warm-paper + indigo `#5E6AD2`; type = Inter + Heebo + Geist Mono | Calm-tech; Hebrew/Latin metric parity; money legibility | Green `#2d5f3f` palette |
| D-002 | 2026-05-30 | Kickoff agreements: alert budget **2/day hard cap**, both adults get everything, weekly review Sat 21:00, nothing off-limits to track | Joint PO session output | — |
| D-001 | 2026-05-25 | Storage = Google Sheets + Drive; dashboard = vanilla PWA; finance = CSV drops (now frozen) | Boring tech; both adults already on Google | — |

## How to add a decision

Append a row at the top with the next ID. If it changes a contract, update `SPEC.md`/`DESIGN.md` in the same commit. If it's a major directional call, both POs decide (see `CLAUDE.md` → Decision authority).
